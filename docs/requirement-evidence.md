# Requirement evidence index

A requirement is marked complete only when the linked commands and artifacts are reproducible. Stage status remains incomplete until every exit condition for that stage passes.

| Requirement | Status | Automated check | Runtime evidence |
|---|---|---|---|
| PLAT-003 | partial | Pi 0.82.0 passes the installed strict extension typecheck, Firstmate's real tmux-backed Calm E2Es, complete baseline, and G0-003 Web/TUI competition; the G0-003 artifact records Node v24.16.0, pnpm 11.17.0, Git 2.54.0, and Herdr 0.7.4; a genuine Slice 3 run adds pinned Treehouse v2.0.1 | `evidence/gate-0/provenance.md` freezes Firstmate; `evidence/slice-3/s3-real-herdr-treehouse-foundation.md` records the Treehouse runtime; full dependency/release certification remains |
| BASE-001 | complete | `tests/gate0-layout.test.sh` asks pnpm to discover the plain workspace package set | `evidence/gate-0/workspace.md` |
| BASE-002 | partial | `tests/gate0-layout.test.sh` verifies every required source boundary; `tests/gate0-primary-launch.test.sh` exercises the bounded launcher/server paths | `evidence/gate-0/workspace.md`; full product schemas and later-slice package functionality remain |
| BASE-003 | complete | `tests/gate0-tracking.test.sh` verifies an ordinary root tree, direct tracking, and rejects nested Git, gitlink, subtree metadata, and patch-stack mechanisms; the layout test retains a focused worktree scan | `evidence/gate-0/provenance.md`, `evidence/gate-0/workspace.md` |
| BASE-004 | complete | `tests/gate0-tracking.test.sh` verifies operational paths are ignored and absent from root HEAD/index; launcher fixes `FM_HOME` and `FM_ROOT_OVERRIDE` to canonical `firstmate/` | `evidence/gate-0/provenance.md`, `evidence/gate-0/init.md` |
| PATH-001 | partial | `bin/clerkmesh` canonicalizes the root and exports all required absolute paths | External-input containment checks remain |
| PATH-002 | complete | Launcher resolves product paths from its canonical location, independent of caller cwd | `evidence/gate-0/init.md` |
| INIT-001 | complete | `tests/gate0-tracking.test.sh` validates the frozen repository/branch/commit/tree, import method/time, license digest, and attribution without network access | `firstmate.provenance.json`, `firstmate/LICENSE`, `evidence/gate-0/provenance.md` |
| INIT-002 | complete | Provenance source commit differs from and supersedes research SHA | `evidence/gate-0/provenance.md` |
| INIT-003 | partial | `tests/init.test.sh` covers explicit/repeated init, core directories, registry, built-in Escalation Clerk, and init-owned repair validation | Full dependency and layout validation remain |
| INIT-005 | complete | `tests/init.test.sh` covers all seven unmarked operational roots, unsupported versions, conflicting init-owned state, nonconforming path types, and current-version repairs; every refusal snapshots paths and bytes before and after | `evidence/gate-0/init.md` |
| G0-001 | complete | `tests/init.test.sh` passes clean and byte-idempotent repeated init plus the isolated 18-case unknown/conflict/repair matrix | `evidence/gate-0/init.md` |
| G0-002 | complete | `tests/gate0-tracking.test.sh` validates root tracking, nested-Git/submodule/subtree/patch-stack rejection, provenance invariants, and runtime exclusion | `evidence/gate-0/provenance.md` |
| G0-003 | complete | `tests/gate0-primary-launch.test.sh` catches TUI/Web argv, environment, cwd, and extension divergence; focused Firstmate lock/session-start regressions remain green | `G0_003_LIVE=1 bash tests/cert/g0-003-real-web-tui.sh` passed both real Pi 0.82.0 winner orders; `evidence/gate-0/lock-liveness.md`, `evidence/gate-0/artifacts/g0-003-*` |
| G0-004 | complete | `tests/gate0-no-forge.test.sh` isolates `HOME`/Git config, restricts `PATH`, uses forge executables only as failing invocation tripwires, and drives real session-start, Project preflight, local Git, and spawn-refusal paths | `evidence/gate-0/no-forge.md` |
| PROJ-003 | partial | The G0-004 test proves ordinary startup makes no forge readiness probe and dispatch preflight is mode-specific; `test:slice3-project-catalog` proves both Project addition and mode switching run the target mode's read-only preflight against staged authority before atomic publication, leave the registry byte-identical on refusal, and clean up only a newly cloned Project after failed add readiness | Genuine remote certification remains |
| PROJ-004 | partial | `fm-spawn.sh` calls `fm-project-preflight.sh`; G0-004 proves auth refusal occurs before worktree/endpoint/meta creation. The injected Primary protocol requires the same mode-specific read-only preflight before compile/spawn and stops before brief/worktree/endpoint/Worker/`.meta` mutation on failure | Genuine Primary dispatch certification remains |
| COMP-005 | complete | G0-004 proves startup and local-only preflight need no forge account/auth while explicit remote modes fail closed; the genuine S3-001 lifecycle completes without a remote, `gh`, or forge auth | `evidence/slice-3/s3-001-local-delivery.md` |
| G0-005 | complete | `corepack pnpm run test:firstmate` passes all 96 upstream scripts with 0 failures and 15 declared gate skips; no failure was waived | `evidence/gate-0/firstmate-baseline.md`, focused fixture evidence, and the historical diagnostic artifact at `evidence/gate-0/artifacts/firstmate-baseline.json` |
| PROC-001 | partial | `bin/clerkmesh web` now launches the single composed foreground product application, defaulting to loopback with no daemon/restart behavior | `evidence/gate-0/lock-liveness.md`; real Slice 1 product certification remains |
| PROC-002 | partial | `test:slice1-schema` proves the composed product application binds Fastify close to exactly one owned-child SIGTERM, shares idempotent SIGINT/SIGTERM cleanup, and closes on listener failure without any Worker ownership dependency. `test:slice3-project-catalog` additionally proves unexpected Primary exit leaves Worker polling active, Web exit stops only process-local polling, and Web restart recovers external Worker facts without inventing a Primary or completion | `cert:slice1-shutdown-offline` proves Web shutdown terminates its genuine Pi child while an independently live Worker survives; Learning Agent ownership remains Slice 5 work; `evidence/slice-1/s1-006-shutdown-offline.md` |
| PROC-003 | partial | Fast contract proves Web/TUI use one canonical home and explicitly load the same Firstmate and ClerkMesh Primary extensions; real Gate-0 certification covers the shared Firstmate extension launch baseline | Real certification with the ClerkMesh Primary Extension remains CERT-002 |
| CONV-001 | complete | `test:slice1-schema` freezes the opaque, path-free session catalog boundary and verifies server discovery uses the pinned Pi 0.82.0 `SessionManager.listAll` metadata API, canonicalizes cwd/symlink aliases, omits foreign/invalid sessions, fails closed on duplicate IDs, and serves the Ajv-validated projection over Fastify without a Primary launch dependency; the production React client build browses this catalog and retains only the opaque selection in URL state, while the guarded product Fastify process serves the built shell and containment-checked immutable assets | `cert:slice1-history-browse` parses an isolated persisted Captain history through real Pi 0.82.0 metadata discovery, filters foreign cwd through Fastify, and proves the Primary/provider tripwire remains untouched; `evidence/slice-1/s1-001-history-browse.md` |
| CONV-002 | complete | `test:slice1-schema` proves concurrent HTTP first sends share one startup promise, resolve opaque session IDs server-side, reject path-bearing mutation input, lock selection once startup begins, and never prompt before startup succeeds; before send, the client keeps selected history solely in browser URL state | `cert:slice1-concurrent-first-send` concurrently replays one request through a genuine Pi 0.82.0 RPC child and proves exactly one visible Captain message, one model reply, and `agent_settled`; `evidence/slice-1/s1-002-concurrent-first-send.md` |
| CONV-003 | complete | The product application composes one shared supervisor/projection/idempotency map/lease, keeps catalog queries launch-free, stops its owned RPC child on Fastify close, projects child exit as offline, refuses subsequent startup/prompt attempts without auto-restart, and locks selection after startup begins | `cert:slice1-shutdown-offline` externally terminates a genuine Pi child, observes offline, proves a later mutation returns 503, and verifies no replacement PID appears; `evidence/slice-1/s1-006-shutdown-offline.md` |
| CONV-005 | partial | `test:slice1-schema` proves one WebSocket carries lease state, projection events, extension UI, and periodic heartbeat frames while HTTP remains the query/mutation transport; the production client consumes that socket and sends mutations over HTTP; no SSE transport exists | `cert:slice1-stream-extension-ui` proves genuine Pi stream and extension UI enter the shared projection; browser refresh remains |
| CONV-004 | complete | `test:slice1-schema` binds normalized conversation events to Pi 0.82 RPC event shapes without parsing terminal/ANSI output, proves strict bounded JSONL command correlation/event forwarding, and integrates that client and normalizer through the single-child product supervisor | `cert:slice1-stream-extension-ui` proves genuine Pi RPC visible stream, durable reply, extension UI, and `agent_settled`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| CONV-006 | complete | `test:slice1-schema` proves process-local `(client token, requestId)` HTTP replay returns the original result without a duplicate prompt, while conflicting reuse returns 409 | `cert:slice1-reconnect-idempotency` proves concurrent replay and conflict handling over real loopback HTTP; the guarantee is explicitly process-local and ends at restart; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| CONV-007 | complete | `test:slice1-schema` uses fake-clock unit and real loopback WebSocket/HTTP integration to prove first-token acquisition, multiple same-token connections, a strict three-second reconnect reservation, second-token HTTP 409 refusal, explicit claim after expiry, and vacant new-connection acquisition; the production client uses a stable opaque browser token, displays lease state, disables read-only mutation, and exposes explicit claim | `cert:slice1-reconnect-idempotency` uses browser-equivalent real WebSocket/HTTP connections and real wall-clock expiry to prove same-token reconnect, second-token refusal, post-expiry explicit claim, and old-owner refusal; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| CONV-008 | complete | The WebSocket lease accepts opaque local client tokens only and exposes no actor/authentication semantics; HTTP mutation uses the token solely to check writability; the production client labels state and claims only as a local write lease and explicitly says it is not authentication or identity | `test:slice1-schema` checks both source semantics and the built production client |
| CONV-009 | complete | `test:slice1-schema` proves a versioned, schema-valid process-local projection retains at most 10,000 normalized events, assigns monotonic sequence cursors, retains pending extension UI, and sends an initial runtime-validated WebSocket snapshot followed by live monotonic continuations without exposing diagnostics by default; selected-session startup rebuilds only durable user/assistant visible messages from Pi `get_messages` and does not synthesize tool results, reasoning, stream fragments, statuses, or diagnostics; the production client deduplicates snapshots by sequence and retains the bounded visible timeline | `cert:slice1-refresh-restart` proves a browser-equivalent refresh receives genuine Pi extension UI from the live process snapshot, then a new Web process reconstructs durable Captain history through genuine Pi RPC without inventing prior extension UI, stream fragments, or diagnostics; `evidence/slice-1/s1-005-refresh-restart.md` |
| CONV-010 | partial | `test:slice1-schema` proves ordinary snapshots exclude diagnostic events while explicit diagnostic snapshots include them, with defensive payload copies; Pi 0.82 RPC fixtures project visible text/stream/status/extension UI, keep reasoning/tool lifecycles diagnostic-only, redact credential-shaped fields, whole environment maps, and Bearer values before storage, and require extension command discovery before prompt acceptance; the production client keeps diagnostics off by default and requires an explicit warning-bearing opt-in before subscribing to and rendering redacted diagnostic detail; the real Primary Extension registers the required command and injects its complete tracked behavior protocol exactly once per run | Real diagnostic rendering remains |
| EXEC-001 | complete | `test:slice1-schema` proves the Primary Extension appends the complete tracked `CLERK.md` exactly once on each `before_agent_start` run and registers `/clerkmesh-status`; the protocol names the cwd-independent inspect/index/shortlist and context-compile control-plane calls, requires their actual execution, and stops before spawn/readiness on command failure while explicitly retaining selection, brief, spawn, and assignment authority outside the extension; `tests/gate0-primary-launch.test.sh` proves both canonical Primary modes load it | `cert:slice1-stream-extension-ui` proves the canonical genuine Pi child loads and executes the extension before a real settled agent run |
| EXEC-002 | complete | The strict RPC client rejects prompting unless extension-sourced `/clerkmesh-status` is discovered, the extension contract proves the matching command is registered, and the canonical Web/RPC launch includes that extension | `cert:slice1-stream-extension-ui` starts genuine Pi, passes command discovery, and invokes the command's real UI notification |
| EXEC-003 | complete | The tracked behavior protocol and extension implementation contain no selection, input modification, spawn interception, brief write, or assignment state; the real Firstmate Pi Worker launch contract loads only its task-scoped turn-end extension and excludes the ClerkMesh Primary Extension and its command | `cert:slice1-stream-extension-ui` proves the extension behavior at the canonical Primary boundary; Worker exclusion is verified at Firstmate's concrete launch-command boundary |
| CONV-011 | partial | Real competition proves the Web seam leaves lock authority to Firstmate and the loser reports read-only | Full Conversation Bridge remains Slice 1 |
| UI-001 | partial | The production React build provides the calm Conversations object workspace with a focused session list, detail breadcrumb, and responsive layout | Remaining top-level workspaces belong to later slices |
| UI-002 | partial | The Slice 1 client exposes only Conversations and hides all later-Slice navigation and controls | Later slices remain unimplemented |
| SEC-003 | partial | RPC normalization redacts credential-shaped fields, whole environment maps, and Bearer values before projection; the production diagnostic view is opt-in, visibly warns about sensitive operational detail, and ordinary mode receives no diagnostic events | Full real information-leakage certification remains |
| SEC-005 | complete | The injected Primary protocol defines `yolo` only as bounded delivery automation and requires explicit Captain confirmation for destructive, security-sensitive, irreversible, or out-of-request decisions; it also prohibits bypassing Firstmate's cleanliness, exact-tip, and fast-forward landing safeguards. The local landing matrix proves yolo-off approval refusal and yolo-on divergence refusal | `S3_002_LIVE=1 corepack pnpm run cert:slice3-yolo-escalation` passed with a genuine Pi 0.82.0 Primary that awaited explicit Captain confirmation and did not reach the destructive credential/data tripwire; `evidence/slice-3/s3-002-yolo-escalation.md` |
| SEC-001 | partial | `bin/clerkmesh web` binds the production entrypoint to the guarded listener; `test:slice1-schema` proves it defaults to `127.0.0.1`, warns conspicuously before a deliberate non-loopback bind, and every conversation route rejects non-loopback or malformed Host, forwarded Host, DNS-rebinding authorities, and non-loopback/null Origin before handlers; HTTP mutations separately require JSON Content-Type | Production entrypoint and browser security certification remain |
| READ-003 | partial | Conversation schemas, `clerkmesh.execution-context.v1`, `clerk-catalog.v1`, `fm-task-graph.v1`, `fm-project-catalog.v1`, `fm-herdr-agents.v1`, `fm-task-activity.v1`, and the transient `clerkmesh-task-detail.v1` composition are versioned with reproducibly generated TypeScript types; schema contracts reject private runtime identifiers and paths, while existing HTTP boundaries perform Ajv runtime validation | Remaining activity schema and new projection HTTP bindings remain |
| CERT-001 | complete | Opt-in real runner uses Pi 0.82.0, real tmux/TUI, and real Pi RPC through the ClerkMesh Web process path | `evidence/gate-0/artifacts/g0-003-real-web-tui.txt` |
| S1-003 | complete | The opt-in runner starts one genuine Pi RPC Primary and requires extension command discovery, extension UI, non-empty visible stream, durable reply, and `agent_settled` | `S1_003_LIVE=1 corepack pnpm run cert:slice1-stream-extension-ui`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| S1-004 | complete | Contract tests cover lease edge cases and the tracked certification drives browser-equivalent real loopback WebSocket and HTTP transports with wall-clock reconnect expiry | `corepack pnpm run cert:slice1-reconnect-idempotency`; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| S1-005 | complete | Projection and reconstruction contracts are exercised by `test:slice1-schema`; the tracked certification uses genuine Pi RPC and browser-equivalent WebSocket refresh | `corepack pnpm run cert:slice1-refresh-restart`; `evidence/slice-1/s1-005-refresh-restart.md` |
| S1-006 | complete | Supervisor and application contracts prove owned-child cleanup, Worker-independent shutdown, offline projection, and no auto-restart | `corepack pnpm run cert:slice1-shutdown-offline` uses genuine Pi children and an independent live Worker ownership tripwire; `evidence/slice-1/s1-006-shutdown-offline.md` |
| CERT-002 | complete | Contract tests verify complete once-per-run protocol injection; the opt-in real runner verifies canonical extension loading, command discovery, extension UI, Pi RPC stream, and settled model execution | `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| CERT-004 | complete | The isolated genuine Pi/Herdr/Treehouse run uses real Git for local-only worktree execution, validation, authoritative review, guarded fast-forward landing, landed proof, and teardown | `evidence/slice-3/s3-001-local-delivery.md` |
| CERT-006 | complete | Guarded genuine runs against the Captain-authorized private fixture exercised production direct-PR and no-mistakes preflights, branch-specific PR creation, immutable head/ancestry verification, complete-diff review, and squash merge | `evidence/slice-3/s3-007-remote-certification-blocker.md`; `evidence/slice-3/artifacts/s3-007-real-direct-pr.txt`; `evidence/slice-3/artifacts/s3-007-real-no-mistakes.txt` |
| CLERK-001 | partial | `test:slice2-clerk-repository` freezes and verifies the strict repository contract: independent Git metadata, exact frontmatter, ordered six-section `CLERK.md`, size limits, approved root layout, indexed material metadata, Skill structure, and symlink/nested-repository refusal | Lifecycle integration remains |
| CLERK-002 | partial | `test:slice2-clerk-repository` validates an isolated real-Git approved commit snapshot, proves dirty working-tree content cannot participate, and fails closed when the requested historical commit is absent | Lifecycle selection/compile binding and full history-object certification remain |
| CLERK-003 | partial | `test:slice2-clerk-repository` freezes the strict registry v1 table parser/renderer, canonical `clerks/<name>` containment, unique names, statuses, built-in Escalation invariants, and validated atomic publication | Lifecycle mutation and same-name non-reuse remain |
| CLERK-004 | complete | Registry and lifecycle contracts keep the built-in human Escalation Clerk active, unique, and unarchivable; the Primary protocol prohibits automatic selection | `S2_007_LIVE=1 corepack pnpm run cert:slice2-clarification-escalation` passed with a genuine Pi Primary and selected Escalation only after explicit Captain takeover; `evidence/slice-2/s2-007-clarification-escalation.md` |
| CLERK-005 | partial | `test:slice2-clerk-repository` rejects executable material outside Skills and rejects every Skill auxiliary file not explicitly referenced by its `SKILL.md`, while permitting unindexed raw Sources | Commit approval semantics and capability permission containment remain |
| LIFE-001 | complete | `test:slice2-clerk-repository` exercises all eight narrow Agent-facing create/register/validate/inspect/review/commit/archive/restore commands without a dispatcher | Full lifecycle integration remains |
| LIFE-002 | complete | Every lifecycle command emits only compact tab, Markdown, or requested diff output on success, diagnostics only on stderr, and standard zero/nonzero status; failures emit no stdout | CLI output contract is covered |
| LIFE-003 | complete | `test:slice2-clerk-repository` proves the shared lifecycle mutation lock refuses a live PID, reclaims a dead owner, fails closed on malformed ownership, and ownership-checks release; every lifecycle mutation, including commit, binds to that authority | A real forcibly killed archive mutation leaves a stale process lock that restart safely reclaims; repeated at the Slice 3 boundary in `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| LIFE-004 | complete | `test:slice2-clerk-repository` proves registry replacement uses validated atomic rename; create/register stage repositories safely; creation and registration journal the repository-to-registry publication gap; archive and restore journal intended registry transitions before publication; all four deterministically complete interrupted publication on restart; malformed journal state fails closed; and commit validates both immutable endpoints and the reviewed candidate tree before a Git `update-ref` compare-and-swap, refusing tree mismatch and HEAD races without moving the ref | A real `SIGKILL` between durable intent and completion is recovered by the next locked lifecycle invocation; the archive/restore/CAS/recovery boundary was repeated for S3-006 in `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| S2-001 | complete | `test:slice2-clerk-repository` verifies repository and registry contracts against isolated fixtures and real Git: create/validate/register produce valid independent repositories, dirty/untracked source content is excluded, interrupted publication recovers, immutable review and CAS commit remain fixed-tree, and archive/restore plus live-lock refusal pass | Real kill/restart recovery evidence: `evidence/slice-2/s2-001-clerk-lifecycle.md` |
| S2-002 | complete | `test:slice1-schema` proves complete once-per-run Primary injection and canonical launch loading; `test:slice2-clerk-repository` additionally drives Firstmate's concrete Pi Worker spawn and proves its argv loads only the task-scoped Worker extension, never the ClerkMesh Primary Extension or `/clerkmesh-status` | Existing real Pi `cert:slice1-stream-extension-ui` evidence proves canonical Primary extension loading, command discovery, complete injection, and a settled run; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| S2-003 | complete | Frozen schema, immutable compiler, atomic brief integration, narrow commands, and fail-closed injected protocol pass in `test:slice2-clerk-repository`; the opt-in runner independently validates the resulting payload | `S2_003_LIVE=1 corepack pnpm run cert:slice2-primary-selection` passed with genuine Pi 0.82.0 semantic selection and compilation; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-004 | complete | The injected protocol requires candidates → shortlist → semantic selection → compile through the narrow immutable-commit commands and forbids descriptive substitution; the isolated certification presents competing active Agent Clerks | A genuine Pi 0.82.0 Primary selected the semantically appropriate review Clerk and compiled its context; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-007 | complete | `test:slice2-clerk-repository` freezes the strict `clerkmesh.execution-context.v1` payload with Task ID, Clerk name/execution/full commit, identity context, selection reason/boundaries, and approved material path/blob allowlist; generated TypeScript freshness is checked; the compiler binds identity and Primary-explicitly-selected material blob OIDs to an immutable approved commit, refuses omitted, duplicate, unsupported, or absent material selections and HEAD drift, excludes Sources and non-readable paths, emits deterministic canonical JSON/base64/SHA-256, and atomically inserts one standard replaceable machine block into an existing brief; a cwd-independent Primary-facing command exposes that exact pipeline with compact identity/hash output and failure-without-mutation coverage | `cert:slice2-primary-selection` independently verified the genuine Primary's canonical payload hash, Task, selected Clerk, approved commit, and explicit blob allowlist; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-008 | complete | Compiler and atomic brief integration tests require the selected Clerk HEAD to remain the approved commit immediately before publication, preserve existing brief prose, and copy no material body or assignment file | The real S2-003 run compiled the standard machine block into an existing isolated brief and independently matched its commit and hash; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-005 | complete | The injected protocol requires ambiguous, unsafe-to-split, and unmatched requests to remain in conversation for clarification and forbids automatic Escalation selection | A genuine Pi Primary inspected candidates, left an unsafe underspecified request in conversation without compiling a brief, and selected Escalation only after explicit Captain takeover; `evidence/slice-2/s2-007-clarification-escalation.md` |
| EXEC-006 | complete | The injected protocol defaults to automatic planning but requires a requested split/selection/reason/boundary preview to wait for Captain response | The genuine S2-007 run presented requested handling, Clerk, reason, and boundary information, then waited without brief mutation until the next Captain turn; `evidence/slice-2/s2-007-clarification-escalation.md` |
| EXEC-009 | complete | `test:slice2-clerk-repository` proves a current Worker capability remains fixed to its original brief hash, commit, and blob allowlist across follow-up use, while a newly published execution context creates a distinct capability and cannot access the prior allowlist; `cert:slice2-worker-continuity` exercises the same boundary with genuine Workers | A genuine Pi session retained the old immutable capability across its initial turn and follow-up, while a distinct Herdr/Pi Worker used the rotated context and refused the old path; `evidence/slice-2/s2-005-worker-continuity.md` |
| EXEC-010 | complete | `test:slice2-clerk-repository` fail-closed parses exactly one canonical brief machine block, verifies its canonical base64/SHA-256 integrity and strict Agent context, rejects duplicate entries, Sources, scripts, unsupported paths and traversal, and exposes bounded Worker-facing `list/search/read` only against allowlisted immutable Git blobs while excluding dirty working-tree content; the injected Primary protocol requires the existing brief to give Workers the concrete bounded command before ordinary spawn and forbids copied material bodies or parallel context/assignment records | A genuine Herdr/Pi Worker listed and read only the immutable allowlisted blob while Source, script, and traversal reads were refused; `evidence/slice-2/s2-004-worker-capability.md` |
| EXEC-011 | complete | The cwd-independent Worker capability command derives its repository only beneath the canonical Clerk root and writes content-free JSONL audit records for allowed and refused operations beneath the canonical state root in `capabilities/<context-sha256>.jsonl`; tests prove audit contains context identity but no dirty material and refuse relative, aliased, or symlinked Clerk/state/capability roots; the Primary protocol names this command as the sole Worker material interface while keeping capability state out of the brief | The genuine Worker run independently verified allowed/refused content-free audit records under the canonical state root without leaking refused content; `evidence/slice-2/s2-004-worker-capability.md` |
| S2-004 | complete | Canonical reader, immutable capability operations, bounded Worker command, audit containment, Primary brief instructions, and the concrete Firstmate Worker extension boundary pass in `test:slice2-clerk-repository` | `S2_004_LIVE=1 corepack pnpm run cert:slice2-worker-capability` passed with a genuine Herdr 0.7.4/Pi 0.82.0 Worker; `evidence/slice-2/s2-004-worker-capability.md` |
| S2-005 | complete | `test:slice2-clerk-repository` covers immutable follow-up capability retention and new-execution hash/commit/allowlist rotation without cross-snapshot access; the opt-in runner drives two turns in one Pi session and a distinct new Herdr/Pi Worker | `S2_005_LIVE=1 corepack pnpm run cert:slice2-worker-continuity` passed with genuine Herdr 0.7.4/Pi 0.82.0 Workers; `evidence/slice-2/s2-005-worker-continuity.md` |
| S2-006 | complete | `cert:slice2-task-detail` verifies schema-valid path-free Clerk catalog and Firstmate Task graph projections, guarded HTTP validation, integrity-checked transient Task-detail composition, nullable fail-closed enrichment, and absence of Clerk ownership fields from Firstmate's production control plane | `evidence/slice-2/s2-006-task-detail-projection.md` |
| S2-007 | complete | Extension and repository contracts enforce clarification, preview waiting, and explicit-only Escalation selection; the opt-in runner verifies all three turns and byte-identical brief retention | `S2_007_LIVE=1 corepack pnpm run cert:slice2-clarification-escalation` passed with a genuine Pi Primary; `evidence/slice-2/s2-007-clarification-escalation.md` |
| LIFE-005 | complete | `clerk-catalog.v1` freezes a path-free Web boundary distinct from registry Markdown, with generated TypeScript freshness checks; the server projector consumes only the canonical registry parser and the guarded `/api/clerks` query runtime-validates its path-free approved-commit projection | Focused stage evidence: `evidence/slice-2/s2-006-task-detail-projection.md` |
| READ-002 | partial | `clerkmesh.execution-context.v1` and `clerk-catalog.v1` have versioned shared schemas and reproducibly generated TypeScript interfaces | Catalog projector binding and remaining ClerkMesh read models remain |
| READ-004 | partial | `clerk-catalog.v1` explicitly carries observed time, `current|unknown` freshness, registry provenance, omissions, and errors; projection tests prove registry failure becomes path-free unknown/error state and invalid approved commits become explicit omissions. The Firstmate Task graph projector likewise emits provenance, freshness, duplicate/invalid-task omissions, inventory errors, safe result links, and no private paths or endpoint detail. | Remaining read models remain |
| READ-005 | complete | `fm-task-graph.v1` projects a Clerk-agnostic Task boundary from Firstmate's authoritative fleet semantics. The guarded Web Task-detail endpoint queries that projection by Task ID and transiently enriches it only from the integrity-checked standard execution-context block in Firstmate's current brief, returning nullable immutable Clerk identity with explicit current-or-most-recent semantics and dual provenance; missing, malformed, mismatched, or symlinked briefs disclose no paths and never create ownership state | `test:slice2-clerk-repository` |
| READ-007 | complete | `test:slice3-project-catalog` proves production composition binds shared Task graph and Herdr Agent pollers to their public commands, starts the two-second schema/hash-gated queries only while the production Work client explicitly subscribes over the guarded WebSocket, publishes only schema-valid semantic fact changes (not per-query `observedAt` churn), and stops after the final subscriber disconnects; the built Work view composes Task and Agent runtime facts without private reads | A genuine Herdr/Treehouse E2E changed the live endpoint set from one to two; the production command, schema validator, semantic hash gate, two-second poller, and guarded WebSocket published the changed projection after 2050 ms without private endpoint or terminal fields; `evidence/slice-3/s3-004-real-web-polling.md` |
| READ-008 | partial | Polling query/schema failures publish a sanitized explicit error while retaining the last successful snapshot and its observation time; the production Work view keeps that snapshot visible and labels Task or Agent failures stale with the failed observation time rather than presenting current facts | Genuine runtime stale/error evidence remains |
| READ-001 | complete | All four required v1 interfaces have strict shared schemas and generated TypeScript types; production Project catalog, Task graph, Herdr Agent, and bounded cursor-based Task activity projectors are covered for schema validity, current/stale/unknown freshness, omissions/errors, path-free output, and fail-closed malformed inputs | `corepack pnpm run cert:slice3-projections`; `evidence/slice-3/s3-003-firstmate-projections.md` |
| READ-006 | complete | `test:slice3-project-catalog` proves the Firstmate-owned append boundary emits only validated structured event types with contiguous cursors, Firstmate observation time, nullable genuine occurrence time, bounded summaries, durable fsync, and fail-closed task/history containment; malformed history remains unchanged. Ordinary ship and scout brief creation now appends `task-created` only after the brief exists, rolls the brief back if activity recording fails, and excludes non-Task secondmate charters. Successful ship/scout spawn now appends `dispatch-started` only after the launch is sent, fails visibly if durable recording fails, and excludes secondmate launches. Successful local-only fast-forward landing now durably appends `landed`, fails visibly if recording fails, and refused/diverged landing emits no false event. Successful ordinary Task teardown appends `teardown-recorded` before retiring volatile task metadata, preserves metadata for diagnosis if recording fails, excludes secondmate infrastructure, and refused teardown emits no false event. Authoritative Task diff review now durably appends `delivery-reviewed` after complete output and fails visibly if recording is unavailable. Creating or retrying a durable Captain hold appends exactly one `decision-requested` event per decision after the hold is active; failures remain visible and retries repair a missing append. Durable Captain decision resolution similarly appends exactly one idempotent `decision-recorded` event after routing and hold closure. Worker briefs now route status reports through a bounded Firstmate command that preserves the legacy status wake log and appends `worker-status-observed` with a Firstmate `observedAt` and no fabricated occurrence time; malformed summaries, unsafe storage, and symlinked logs fail closed. Accepted passed or failed Project validation results now use a bounded Firstmate command that appends `validation-recorded` with compact output, authoritative observation time, and fail-closed Task containment. Successful approved remote PR merge now appends `landed` only after `gh-axi` accepts the merge; forge refusal emits no false landing and post-merge activity failure remains visible | `corepack pnpm run cert:slice3-projections`; `evidence/slice-3/s3-003-firstmate-projections.md` |
| PROJ-001 | partial | `test:slice3-project-catalog` proves Firstmate projects and `data/projects.md` remain authoritative while its read-only projector preserves registered/discovered/missing semantics, validates managed directories and Git roots, and exposes no filesystem paths or second registry; the deterministic init command creates only beneath the managed Firstmate Project root and registers there last | Real spawn/delivery lifecycle remains |
| PROJ-002 | complete | `test:slice3-project-catalog` proves an untagged registered Project defaults to `local-only off` in both dispatch preflight and the Project catalog, even when its Git repository has an origin; remote modes therefore require explicit registry authorization | Focused real-Git contract; ordinary local-only remains independent of forge readiness |
| PROJ-005 | partial | `test:slice3-project-catalog` proves deterministic local-only init creates `main`, a fixed README, a reproducible baseline commit, no remote, and publishes the Firstmate registry entry last; input/preflight refusals are zero-write and later failures retain a diagnosed managed path | Real lifecycle certification remains |
| PROJ-006 | partial | `test:slice3-project-catalog` proves generated local-only Worker briefs exclude GitHub, PR tooling, `gh-axi`, and No Mistakes instructions while retaining project validation and guarded fast-forward delivery requirements | Genuine Worker execution and validation evidence remain |
| PROJ-007 | partial | `test:slice3-project-catalog` drives real Git review, local landing, and teardown, proving the full diff is based on local `main`; `yolo=off` cannot land without the explicit `--captain-approved` gate action; `yolo=on` may auto-approve but cannot bypass clean fast-forward safety; unlanded work is preserved, and only safely landed task state is returned through the worktree-provider boundary | Validation artifacts and genuine runtime landed proof remain |
| PROJ-008 | partial | `test:slice3-project-catalog` proves target-mode readiness is checked before registry mutation, failed switches preserve authoritative bytes, successful switches preserve the orthogonal yolo setting, and existing Task metadata remains fixed to its spawn-time mode while the registry changes for future dispatch | Genuine remote-mode dispatch regression remains |

| HUMAN-001 | complete | `test:slice4-human` freezes the injected Primary rule that a Human Clerk remains in the existing Firstmate Task/brief, must compile the approved Human commit with an explicitly empty material allowlist, and creates no Worker, wake, endpoint, capability, worktree, parallel human record, or synthetic Agent status; report publication requires a matching integrity-checked Human execution context; an isolated lifecycle integration proves accepted publication leaves only the authoritative brief/report and a fresh Task-detail composition recovers Human Clerk context without inventing Worker/Agent facts; Work explicitly presents Human execution as creating no Worker. | A genuine Pi Primary selected the ordinary Human Clerk, compiled its immutable empty-allowlist context, accepted Captain-relayed evidence, and created no Worker runtime artifact or queued wake; `evidence/slice-4/s4-001-human-execution.md` |
| HUMAN-002 | complete | `test:slice4-human` requires visible Captain-relayed start/progress/questions/evidence/result and requires Primary to invoke the bounded report command with exact relayed Markdown, explicitly evaluate every acceptance criterion, reserve `accepted` for satisfaction, and fail closed without a publication claim; the command atomically publishes non-empty Markdown with fixed local Captain actor provenance plus explicit `accepted|rejected|incomplete` evaluation; lifecycle integration preserves the accepted report across a refused empty replacement; invalid outcomes, empty results, unsafe Tasks, and symlinked reports fail without mutation | The genuine S4-001 run published the exact relayed Markdown with local Captain provenance only after evaluating both acceptance criteria; `evidence/slice-4/s4-001-human-execution.md` |
| HUMAN-003 | covered | `test:slice4-human` requires Project, Git, shell, or tool changes to preserve the accepted Human Task/report as dependency authority; actually create a separate Firstmate Agent Task through the pinned `tasks-axi` with `--blocked-by <human-task-id>`; verify the edge through Firstmate's Task projection; and restart candidates → shortlist → semantic Agent Clerk selection, Project preflight, and Agent context compilation before ordinary spawn. The isolated real-control-plane fixture proves a separate Agent Task is authoritatively blocked by the Human Task, the public graph exposes that exact edge, and a missing dependency is refused without backlog mutation. Any failed control-plane step stops before spawn, and prose or a parallel ClerkMesh dependency record is forbidden | `S4_003_LIVE=1 corepack pnpm run cert:slice4-human-agent-handoff`; tracked passing evidence: `evidence/slice-4/s4-003-human-agent-handoff.md` |
| HUMAN-004 | complete | The protocol explicitly distinguishes the Captain as the local relay rather than an account or Clerk; report publication records only fixed local decision provenance; Work describes only the current-or-most-recent execution Clerk and contract coverage rejects owner or assignment language | Genuine ordinary Human and explicit Escalation fixtures introduce no User, owner, assignment, or permission semantics; `evidence/slice-4/s4-002-human-escalation-selection.md` |

| LEARN-001 | partial | `test:slice5-learning` establishes the immutable content-addressed Source capture boundary: only explicit Captain import, accepted Human Task evidence, or explicit agent-generated re-import can persist; Agent completion/report/wake and extraction result origins fail before writes. Human provenance requires an accepted outcome and report hash, while explicit agent re-import is permanently warning-marked. | Proposal integration, production command/API binding, and genuine scenario evidence remain |
| LEARN-002 | partial | `test:slice5-learning` preserves `agentGenerated: true` plus a visible caution on an explicit Captain re-import and rejects treating ordinary Agent events as that explicit action. | Reviews UI warning and genuine scenario evidence remain |
| LEARN-003 | partial | `test:slice5-learning` creates one authoritative Proposal over two distinct active Agent Clerks, fixes each target independently to its canonical Git HEAD, provisions isolated full candidate clones, and atomically advances all targets to extracting only after independent runtime endpoints are persisted. | Per-target review/decision state and genuine runtime evidence remain |

## Slice 2 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S2-001 | CLERK-001–005, LIFE-001–004 | isolated real-Git lifecycle integration, dirty-tree and recovery cases | Complete: valid independent repositories, dirty exclusion, lifecycle mutation, immutable commits, and real kill/restart recovery are covered |
| S2-002 | EXEC-001–003 | extension run/launch contracts plus real Pi certification | Complete: Slice 1 real Primary evidence plus concrete Firstmate Pi Worker exclusion contract |
| S2-003 | EXEC-004–008 | real Primary selection and canonical execution-context/brief verification | Complete: genuine Pi 0.82.0 selected among competing Clerks and produced an independently verified canonical context block fixed to the approved commit/blob |
| S2-004 | EXEC-010–012 | real Worker capability allowlist/refusal/audit certification | Complete: a genuine Herdr/Pi Worker used the immutable allowlist capability and produced content-free allowed/refused audit evidence |
| S2-005 | EXEC-009 | real follow-up and new-execution capability rotation | Complete: one genuine Pi session retained its immutable snapshot across follow-up and a distinct Herdr/Pi Worker rotated capability with cross-snapshot refusal |
| S2-006 | LIFE-005, READ-002–005 | runtime-validated catalog and Task-detail composition | Complete: focused certification proves the path-free catalog, Clerk-agnostic Task graph, guarded HTTP boundaries, and transient integrity-checked brief enrichment without Firstmate Clerk ownership fields |
| S2-007 | CLERK-004, EXEC-005–006 | real clarification and explicit Escalation selection transcript | Complete: genuine Pi Primary clarified unmatched work, waited after a requested preview, and selected Escalation only after explicit takeover without brief compilation |

## Slice 3 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S3-001 | PROJ-001, PROJ-002, PROJ-004–007, EXEC-012 | real local-only init → spawn → validate → review → approve → fast-forward → teardown | **Complete:** the isolated genuine Pi/Herdr/Treehouse runner produced a clean validated Worker commit, disclosed its authoritative diff, refused unapproved yolo-off landing, fast-forwarded the exact reviewed tip after explicit approval, proved landing, and safely tore down without remote, `gh`, or forge auth; `evidence/slice-3/s3-001-local-delivery.md` |
| S3-002 | PROJ-007, SEC-005 | isolated landing refusal/approval matrix for yolo off/on and unsafe changes | **Complete:** yolo-off refuses without explicit approval, approved landing succeeds, yolo-on still refuses divergent history, and a genuine Pi Primary awaited explicit Captain confirmation without reaching a destructive security-sensitive action; `evidence/slice-3/s3-002-yolo-escalation.md` |
| S3-003 | READ-001, READ-003–004, READ-006 | four schema-valid Firstmate projections with freshness/unknown/errors/cursors | **Complete:** `cert:slice3-projections` validates all four production projections against their v1 schemas, including current/stale/unknown freshness, omissions/errors, path-free output, activity continuation, malformed-history refusal, and the durable contiguous-cursor append authority; `evidence/slice-3/s3-003-firstmate-projections.md` |
| S3-004 | READ-007–008, CERT-003 | genuine Herdr + Treehouse projection and two-second hash-change polling | **Complete:** production Work subscriptions activate shared Task graph/Herdr polling, retain last successes, and visibly mark sanitized failures stale; a genuine isolated Herdr/Treehouse run changed endpoint facts from one to two and the production schema/hash-gated poller published the changed snapshot over the guarded WebSocket after 2050 ms; `evidence/slice-3/s3-004-real-web-polling.md` |
| S3-005 | PROC-002, OWN-001, READ-008 | separate Web/Primary/Worker exit and authority recovery scenarios | **Complete:** automated composition coverage proves Primary exit does not stop Worker projections, Web exit stops only process-local polling, and a fresh Web process recovers external Worker facts without inventing a Primary or completion. A genuine isolated Herdr/Treehouse run preserves the same two live endpoint facts and semantic hash across Web exit/restart without launching a Primary; genuine Pi S1-006 evidence separately proves Primary offline/no-restart and owned-child-only shutdown. `evidence/slice-3/s3-005-exit-recovery.md` |
| S3-006 | LIFE-003–004 | archive/restore lock, kill/restart journal, and CAS suite | **Complete:** the Slice 3 boundary certification repeats live-lock refusal, atomic archive/restore, reviewed-tree Git CAS race refusal, and a real post-journal SIGKILL followed by deterministic restart recovery; `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| S3-007 | PROJ-003–004, PROJ-008, COMP-002/005, CERT-006 | isolated direct-PR/no-mistakes regression plus local-only independence | **Complete:** Project-add and mode-switch control planes enforce target preflight before atomic publication, refusal rollback, and existing-Task contract retention. Genuine runs baselined the private fixture, passed both production preflights, reviewed and squash-merged direct-PR PR #1, then drove the complete no-mistakes pipeline, verified certified-input ancestry and the full diff, recorded review, and squash-merged PR #2. See `evidence/slice-3/s3-007-remote-certification-blocker.md` and the three `s3-007-*` artifacts. |

Dependencies: Firstmate remains the sole Project/Task/delivery authority; the Herdr Agent projection reads Firstmate endpoint metadata and queries Herdr without exposing endpoint identities; pinned Treehouse v2.0.1 is available and the complete isolated genuine Pi/Herdr/Treehouse local delivery lifecycle passes (`evidence/slice-3/s3-001-local-delivery.md`); Web models must remain path-free and may not parse Firstmate private files.

Current bounded Project-add coverage: `test:slice3-project-catalog` proves an existing repository is cloned only into an unused managed path and its registry record is atomically published only after mode-specific readiness. Local-only add requires no forge tooling; direct-PR readiness is exercised against an isolated Git transport with `gh`/`gh-axi` tripwires; refusal preserves registry bytes and removes only the newly created clone. No-mistakes initialization is required before its shared readiness check.

Current bounded local delivery coverage: `PATH="$PWD/cache/bin:$PATH" S3_001_LIVE=1 corepack pnpm run cert:slice3-local-delivery` passed the tracked isolated genuine-runtime lifecycle for Pi/Herdr/Treehouse Worker validation, authoritative full-diff review, yolo-off refusal, explicit approval, exact-tip fast-forward landing, and landed teardown. The fixture exposes a Project-owned deterministic completion command so Worker execution and the Git delivery transition are one verifiable action rather than depending on probabilistic multi-step prompt compliance. The injected Primary protocol requires the same mode-specific `fm-project-preflight.sh` used by spawn before compile or spawn, fails before any execution-state mutation, and preserves local-only independence from forge probes. `test:slice3-project-catalog` proves local-only Worker briefs contain no GitHub, PR-tooling, `gh-axi`, or No Mistakes instructions while retaining validation and guarded fast-forward requirements. Valid Project inputs create a no-remote `main` repository with fixed README and deterministic baseline commit before atomically appending its `local-only` Firstmate registry entry. Invalid names, duplicate Projects, and malformed registry authority are refused before any Project write; post-creation failures retain and report the incomplete managed path. A real-Git review/landing contract additionally proves full diff disclosure, yolo-off explicit Captain approval, exact reviewed-tip landing by clean fast-forward, and yolo-on refusal after a divergent-main race. The teardown contract preserves unlanded work and task metadata without invoking Treehouse, then returns the worktree through the provider boundary and removes volatile task authority only after the exact tip is present on local `main`.

## Slice 4 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S4-001 | HUMAN-001–002, OWN-001–003 | isolated Human Clerk Task from visible start through accepted Markdown report, proving absence of Worker/endpoint/capability/worktree and synthetic Agent state | **Complete:** a genuine Pi Primary semantically selected the ordinary Human Clerk, compiled its approved commit with an empty allowlist, evaluated the relayed evidence, atomically published the exact accepted Markdown with Captain provenance, and created no Worker/runtime artifact or wake; `evidence/slice-4/s4-001-human-execution.md` |
| S4-002 | HUMAN-001, HUMAN-004, CLERK-004, EXEC-005–006 | ordinary human selection plus explicit Captain-takeover Escalation transcript | **Complete:** genuine Pi runs prove semantic ordinary Human selection and acceptance without runtime artifacts, while unmatched work remains in clarification and selects Escalation only after explicit Captain takeover; neither path introduces account, owner, assignment, or permission semantics; `evidence/slice-4/s4-002-human-escalation-selection.md` |
| S4-003 | HUMAN-003, EXEC-004, READ-005 | accepted human result followed by a dependent Agent Task and fresh Clerk selection | Passed: pinned real `tasks-axi` creation and the production Task projection prove the separate Agent Task's authoritative dependency edge and mutation-free invalid-dependency refusal; a genuine Pi Primary completed fresh Agent candidate selection, local-only preflight, and canonical context compilation. Evidence: `evidence/slice-4/s4-003-human-agent-handoff.md` |
| S4-004 | HUMAN-001–004, AUTO-005–006 | isolated implementation-Agent-as-test-Captain success, refusal, and restart fixtures retaining conversation, report, projections, and negative Worker evidence | **Complete:** genuine Pi success and refusal conversations plus focused report/recomposition recovery checks retain authoritative report and projection facts while proving no Worker runtime; `evidence/slice-4/s4-004-test-captain-paths.md` |
| S4-005 | PLAN-003, AUTO-005 | complete engineering evidence and tracked `awaiting-final-captain-uat` marker | **Complete:** all preceding Slice 4 exits have reproducible engineering evidence; Slice 4 is explicitly marked `awaiting-final-captain-uat` without gating Slice 5 or representing fixture decisions as Captain approval; `evidence/slice-4/s4-005-awaiting-final-captain-uat.md` |

Dependencies: Firstmate remains the only Task, brief, report, dependency, and delivery authority; Pi Session remains conversation authority; the Clerk approved commit and execution mode remain Clerk Git authority. Human execution must not invoke Herdr, Treehouse, Worker capabilities, or manufacture runtime facts. The bounded `fm-human-report.sh` writes only the existing Task's `report.md` from Captain-relayed Markdown and records Primary's explicit acceptance evaluation; it does not infer acceptance or create runtime state. The existing S2-007 genuine Escalation evidence is reusable only for explicit-takeover selection, not as proof of the ordinary Human Clerk lifecycle.

## Slice 5 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S5-001 | LEARN-001–002 | explicit immutable Source capture plus automatic-learning refusal matrix | **Complete:** production explicit imports and accepted Human reports create immutable Sources; warning and automatic-origin refusal matrices pass; the genuine-Pi isolated fixture verifies exact report bytes and task/hash/Captain provenance; `test:slice5-learning`, `test:slice4-human`, `cert:slice4-human-agent-handoff`; `evidence/slice-5/s5-001-learning-source-capture.md` |
| S5-002 | LEARN-003–004, LEARN-007–008 | genuine two-target extraction with isolated Herdr workspaces and owner-exit continuity | **Complete:** authoritative immutable-base two-target Proposal creation, filesystem/Git-isolated candidate clones, fail-closed Markdown-only enforcement, and exact endpoint persistence pass. The production launcher binds Proposal/Source-derived non-Task Pi policy to one explicit session and one dedicated workspace with independent target tabs/panes. A reproducible genuine Herdr/Pi run proved both Agents continued after the launch owner returned and independently produced validated Markdown candidates without touching canonical Clerk repositories. Evidence: `evidence/slice-5/s5-002-real-learning-extraction.md`. |
| S5-003 | LEARN-005 | complete review, revision invalidation, HEAD race stale, and re-extraction | **Complete:** each target atomically persists complete review material and candidate edits create a new tree with review invalidation. Canonical HEAD races stale only the affected target and clear its review. Re-extraction creates a fresh isolated clone at current canonical HEAD, reuses the Proposal Herdr workspace, replaces endpoint authority, clears stale state, and returns only that target to extraction; `test:slice5-learning`; `evidence/slice-5/s5-003-learning-review-stale-reextract.md`. |
| S5-004 | LEARN-006 | independent approve/reject with approved-target-only CAS commit | **Complete:** reviewed targets independently persist Captain approval or rejection with exact base/tree decision evidence. Approval revalidates Markdown-only output, refuses post-review edits and HEAD races, creates the reviewed tree as a single-parent commit, and compare-and-swap advances only the approved Clerk branch; rejection leaves its Clerk unchanged. After all targets are decided, the authoritative Proposal becomes resolved and refuses further decisions. `test:slice5-learning`; `evidence/slice-5/s5-004-independent-learning-decisions.md`. |
| S5-005 | LEARN-008 | restart reconciliation for live, complete, and interrupted runs | **In progress:** authoritative endpoint reconciliation now preserves live extraction, materializes completed output as review-ready before publication, and records interrupted/failed targets without relaunching or inventing Captain decisions. Production Herdr inspection classifies `done` as complete, active statuses as live, absent panes/Agents as interrupted, and unknown statuses as failed while refusing runtime query uncertainty; production command binding and genuine kill/restart certification remain. |
| S5-006 | AUTO-005–006 | isolated implementation-Agent-as-test-Captain evidence bundle | Not started |
| S5-007 | PLAN-003, AUTO-005 | engineering acceptance marker without fixture data leakage | Not started |

## Stage status

- Gate 0: **complete**
- Slice 1: **complete** (S1-001 through S1-006)
- Slice 2: **complete** (S2-001 through S2-007)
- Slice 3: **complete** (S3-001 through S3-007)
- Slice 4: **awaiting-final-captain-uat** (S4-001 through S4-005 engineering exits complete; Slice 5 is not blocked)
- Slice 5: **in progress** (S5-001–S5-004 complete; S5-005 restart reconciliation is next)
- Release Gate: **not started**
