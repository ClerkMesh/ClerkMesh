# Requirement evidence index

A requirement is marked complete only when the linked commands and artifacts are reproducible. Stage status remains incomplete until every exit condition for that stage passes.

| Requirement | Status | Automated check | Runtime evidence |
|---|---|---|---|
| PLAN-001 | complete | The tracked stage matrix records every Gate 0 and Slice 1–5 exit as complete in strict specification order before opening the Release Gate; Slice 4 first records all preceding exits before explicitly unblocking Slice 5, and Slice 5 records all engineering exits before Release Gate work. The reproducible ordering check verifies every required exit row and the final ordered stage markers | Per-exit automated and genuine-runtime artifacts remain linked from the compact matrices; `evidence/release-gate/plan-001-stage-order.md` |
| PLAN-002 | complete | Independently prepared implementation and fixtures remain behind frozen versioned schemas, generated types, isolated runtime roots, and the tracked stage authority; the reproducible boundary check requires prerequisite-backed Slice 4 and Slice 5 transition artifacts rather than treating later-stage code or tests as Gate passage | Slice 4 explicitly unblocked Slice 5 only after S4-001–004, and Slice 5 opened Release Gate work only after S5-001–006; `evidence/release-gate/plan-002-parallel-preparation-boundary.md` |
| PLAN-003 | complete | The stage-completeness inventory requires Gate 0 and every Slice 1–5 row to retain requirement coverage, an automated result with replay command, genuine-scenario evidence with replay command, and an explicit known-risk boundary; its reproducible check verifies representative completed exits and that the named package commands remain available | Detailed stage artifacts and machine evidence are linked by `evidence/release-gate/plan-003-stage-evidence-completeness.md`; destructive remote replay remains restricted to the Captain-authorized isolated fixture |
| PLAN-004 | complete | The authoritative stage status retains Slice 4 and Slice 5 as `awaiting-final-captain-uat` and the Release Gate as in progress; the acceptance artifacts explicitly prohibit treating internal engineering completion or fixture approvals as a V1 release | The specification permits only a Release Candidate after REL-001 and reserves V1 release for Captain-only REL-002/REL-003; `evidence/release-gate/plan-004-internal-milestones.md` |
| AUTO-001 | complete | The tracked Gate 0 retry record retains each failed genuine-runtime or focused validation attempt, its diagnosis and correction, and repeated final passing reruns instead of recording only a green result; the stage evidence inventory requires replayable checks and known-risk boundaries at every engineering exit | `evidence/release-gate/auto-001-gate-retry-discipline.md`; `evidence/gate-0/lock-liveness.md` records synchronization, observability, cleanup, compatibility, and intermittent-test failures through their corrected passing reruns; final unmet gates remain incomplete rather than waived |
| AUTO-002 | complete | Representative implementation records prove reversible, specification-bounded choices—including isolated Learning clones, temporary-index review trees, completion-marker reconciliation, hashed fixture workspace labels, and additive versioned read metadata—were completed behind focused checks without introducing Captain-only product decisions or approval gates | `evidence/release-gate/auto-002-reversible-implementation-decisions.md`; Captain authority remains reserved for the boundaries named by AUTO-003/AUTO-006 and REL-002/REL-003 |
| AUTO-003 | complete | The tracked escalation inventory restricts Captain requests to specification changes, external credentials, irreversible/destructive operations, and security authorization; ordinary implementation, diagnosis, retries, isolated fixture decisions, and Release Gate work proceed without nightly human gates | `evidence/release-gate/auto-003-captain-escalation-boundary.md`; genuine security-sensitive work stopped for confirmation, destructive remote certification used only the authorized isolated fixture, and Slice 4/5 reserve only final Captain UAT |
| AUTO-004 | complete | The tracked stage record and reproducible ordering check prohibit dependent-stage advancement across an unmet prerequisite; isolated parallel preparation cannot change stage authority, remote certification remained blocked until explicitly authorized, and the Release Gate remains in progress with no Release Candidate or V1 claim | `evidence/release-gate/auto-004-prerequisite-gate-enforcement.md`; Slice 4/5 retain `awaiting-final-captain-uat`, and an ultimately failed prerequisite permits only incomplete-RC blocker evidence rather than a completed dependent Slice |
| AUTO-005 | complete | Slice 4 and Slice 5 completed their focused engineering checks and genuine or isolated-fixture scenarios autonomously without a nightly Captain gate; both acceptance markers preserve `awaiting-final-captain-uat` while allowing the next engineering stage to proceed | `evidence/release-gate/auto-005-slice-engineering-without-human-block.md`; disposable Human and Learning fixture decisions remain test facts, never Captain approval or real business data |
| AUTO-006 | complete | Human Clerk and Learning approve/reject scenarios operate only on disposable fixture authority; their retained artifacts explicitly classify outcomes as engineering test facts rather than Captain UAT or business approval. Production decision provenance distinguishes explicit `captain/local` decisions from automation, and neither Slice marker nor release authority can be advanced by a simulated decision | `evidence/release-gate/auto-006-test-decisions-are-not-captain-decisions.md`; S4-004 and S5-006 test-Captain artifacts prove real Clerk repositories, Firstmate fleet, Projects, Tasks, and business data remain untouched; REL-002/REL-003 remain Captain-only |
| QA-001 | complete | The focused automated suites cover versioned schemas and generated types, deterministic reducers/projections, canonical path and identity handling, bounded Clerk selection inputs, hostile-safe brief encoding, complete Learning state transitions, and fail-closed recovery decisions. The reproducible inventory requires representative checks for every named facet and all five Slice suite commands | `evidence/release-gate/qa-001-unit-contract-coverage.md`; complete clean-root execution remains Release Gate item 3 and is not inferred from this coverage inventory |
| QA-002 | complete | Production CLI integration fixtures use disposable roots and real Git repositories to cover lock contention and stale recovery, kill/restart reconciliation, atomic publication, dirty-tree refusal, CAS races, invalid paths, and fail-closed zero-mutation behavior. The inventory links every required facet to focused Gate 0 or Slice 2–5 tests and verifies the aggregate suite commands remain tracked | `evidence/release-gate/qa-002-cli-integration-coverage.md`; these isolated fixtures never use the product repository or Captain-authorized remote fixture as mutation targets, and the clean-root aggregate run remains Release Gate item 3 |
| QA-003 | complete | Production server integration tests cover validated HTTP queries and mutations, the single guarded WebSocket channel, concurrent and replayed request idempotency, write-lease contention and recovery, canonical hash-gated projections, retained stale/error state, and strict Pi RPC JSONL fixtures. The reproducible inventory links each required facet to focused Slice 1, Slice 3, and Slice 5 checks | `evidence/release-gate/qa-003-server-integration-coverage.md`; genuine HTTP/WebSocket/Pi and stale-runtime scenarios remain linked from CERT-001, CERT-002, and READ-008 evidence, while the clean-root aggregate run remains Release Gate item 3 |
| QA-004 | complete | The complete 96-script vendored Firstmate suite remains the original regression boundary, while focused ClerkMesh tests cover every required changed or consumed surface: lock, bootstrap, brief, preflight, projection, activity, delivery, and teardown. The reproducible inventory requires both original Firstmate tests and ClerkMesh integration fixtures for those boundaries | `evidence/release-gate/qa-004-firstmate-regression-coverage.md`; `evidence/gate-0/firstmate-baseline.md` records the complete baseline with no waived failures, while the clean-root aggregate rerun remains Release Gate item 3 |
| QA-005 | complete | Every genuine-runtime fixture uses disposable authority, guarded non-default sessions, exact teardown, and canonical-root-hash workspace labels. Firstmate's test-only root binding fails closed unless `FM_HOME` is canonically contained while leaving production labels unchanged | `evidence/release-gate/qa-005-test-isolation.md`; `bash firstmate/tests/fm-backend-herdr.test.sh`; `corepack pnpm test:slice5-learning` |
| PLAT-001 | complete | The implementation specification and product metadata define macOS 15+ on Apple Silicon (`arm64`) as the sole supported V1 platform; genuine certifications execute on macOS arm64 rather than substituting portable-source tests for platform evidence | `evidence/slice-2/s2-003-primary-selection.md`, `evidence/slice-3/s3-real-herdr-treehouse-foundation.md` |
| PLAT-002 | complete | No tracked product or release documentation labels Intel macOS, Linux, or Windows supported; portable shell and Node coverage is treated only as implementation coverage | The supported-platform declaration in `IMPLEMENTATION_SPEC.md` remains authoritative |
| PLAT-003 | complete | Pi 0.82.0 passes the installed strict extension typecheck, Firstmate's real tmux-backed Calm E2Es, complete baseline, and G0-003 Web/TUI competition; tracked artifacts record Node v24.16.0, pnpm 11.17.0, Git 2.54.0, Herdr 0.7.4, and Treehouse v2.0.1 rather than treating ambient versions as an implicit contract | `evidence/gate-0/provenance.md` freezes Firstmate; `evidence/gate-0/artifacts/g0-003-real-web-tui.txt` records the core toolchain; `evidence/slice-3/s3-real-herdr-treehouse-foundation.md` records the runtime/provider versions |
| PLAT-004 | complete | Canonical Primary launches and genuine Primary certifications use Pi; Firstmate's concrete Worker launch contract and genuine Workers use Pi; production runtime projections and Learning extraction bind only Herdr; genuine Project lifecycle certification uses the pinned Treehouse provider. No alternate harness/backend/provider is exposed as a ClerkMesh V1 product choice | `test:slice1-schema`, `test:slice2-clerk-repository`, `test:slice3-project-catalog`; `evidence/slice-3/s3-001-local-delivery.md`, `evidence/slice-5/s5-002-real-learning-extraction.md` |
| BASE-001 | complete | `tests/gate0-layout.test.sh` asks pnpm to discover the plain workspace package set | `evidence/gate-0/workspace.md` |
| BASE-002 | complete | `tests/gate0-layout.test.sh` verifies every required source boundary and discovers the root plus all five specified workspace packages; `tests/gate0-primary-launch.test.sh` exercises the bounded launcher/server paths. Slice 1–5 suites exercise the production Web client/server, shared schemas and generated types, Primary extension, Clerk lifecycle/context/Learning control plane, vendored Firstmate integration, and canonical launcher from those exact boundaries | `evidence/gate-0/workspace.md`; completed Slice 1–5 evidence demonstrates the target layout contains the implemented V1 functionality rather than placeholder package boundaries |
| BASE-003 | complete | `tests/gate0-tracking.test.sh` verifies an ordinary root tree, direct tracking, and rejects nested Git, gitlink, subtree metadata, and patch-stack mechanisms; the layout test retains a focused worktree scan | `evidence/gate-0/provenance.md`, `evidence/gate-0/workspace.md` |
| BASE-004 | complete | `tests/gate0-tracking.test.sh` verifies operational paths are ignored and absent from root HEAD/index; launcher fixes `FM_HOME` and `FM_ROOT_OVERRIDE` to canonical `firstmate/` | `evidence/gate-0/provenance.md`, `evidence/gate-0/init.md` |
| BASE-005 | complete | `cert:slice2-task-detail` verifies Firstmate's production control plane contains no Clerk ownership/assignment field; Human Clerk report validation and Learning Source capture live in the ClerkMesh control plane while writing Firstmate's authoritative Task report, leaving direct Firstmate changes limited to generic Task/report, local-only, projection, and activity capabilities | `evidence/slice-2/s2-006-task-detail-projection.md`; Slice 4 report and handoff evidence exercises the ClerkMesh-owned report command |
| PATH-001 | complete | `tests/gate0-primary-launch.test.sh` invokes both public launch paths from an unrelated cwd and verifies all seven required child environment paths equal their canonical absolute product roots | `evidence/gate-0/init.md`, `evidence/gate-0/lock-liveness.md` |
| PATH-002 | complete | Launcher resolves product paths from its canonical location, independent of caller cwd | `evidence/gate-0/init.md` |
| PATH-003 | complete | `tests/init.test.sh` refuses symlinked operational roots without changing their targets, while `tests/gate0-tracking.test.sh` rejects submodules, nested Git repositories, and non-ordinary tracked layouts. Slice 1 session discovery canonicalizes candidate cwd values and excludes aliases or paths outside the product root; Slice 2 Clerk repositories, registries, lifecycle inputs, execution contexts, and capability roots reject traversal, symlinks, nested repositories, and root aliases; Slice 3 Project/catalog/activity boundaries reject unmanaged, escaping, and symlinked paths; Slice 4 reports and Slice 5 Sources, candidates, and target repositories preserve the same canonical containment boundary | The genuine Slice 2–5 fixtures operate only in isolated canonical roots; remote certification uses only the Captain-authorized fixture and never accepts the product repository as an input path |
| OWN-001 | complete | `test:slice1-schema` keeps Pi Session JSONL authoritative for durable conversation history and makes the Web event cache and write lease process-local; `test:slice2-clerk-repository` keeps Clerk identity/version in Git, active/archive state in the sole Clerk registry, and selected execution context in the current Firstmate brief without assignment records; `test:slice3-project-catalog` consumes Firstmate's sole Project/Task/brief/delivery authority and Herdr runtime projection without shadow registries; `test:slice4-human` retains Human work and reports in the same Firstmate Task; `test:slice5-learning` keeps Proposal/review/decision authority in manifests and advances approved learning only through Clerk Git commits | Genuine Pi refresh reconstructs durable conversation solely from its Session; genuine Slice 2 selection, Slice 3 local delivery, Slice 4 Human execution, and Slice 5 extraction/decisions traverse each declared authority without creating a second chat, Task, fleet, Project, assignment, or Learning store; `evidence/slice-1/s1-005-refresh-restart.md`, `evidence/slice-2/s2-003-primary-selection.md`, `evidence/slice-3/s3-001-local-delivery.md`, `evidence/slice-4/s4-001-human-execution.md`, `evidence/slice-5/s5-006-test-captain-decisions.md` |
| OWN-002 | complete | The production Web client exposes no direct Work, Company, or Reviews mutation API. Its sole Work change affordance turns the selected Task intent into an editable draft and navigates back to the current Conversation; submission then uses the guarded conversation HTTP mutation, which projects the text as a visible Captain message and prompts the canonical Primary. Deterministic control-plane commands remain selected and invoked by the Primary protocol rather than by the object workspace | `test:slice3-project-catalog` verifies Task-detail changes create an editable current-conversation draft instead of mutating Task authority; `test:slice1-schema` verifies the submitted message traverses the production HTTP coordinator into the visible conversation projection and Pi Primary prompt; genuine concurrent-first-send evidence proves exactly one Captain message reaches genuine Pi through that path: `evidence/slice-1/s1-002-concurrent-first-send.md` |
| OWN-003 | complete | The durable-decision inventory proves Human Task outcomes, both Learning decision outcomes, and explicit `yolo=off` local landing approval persist the exact fixed local Captain actor object `{"actor":{"type":"captain","id":"local"}}`. Local landing tests additionally prove `yolo=on` automation omits the actor rather than falsely claiming a Captain decision, and the versioned activity projection accepts only the fixed actor shape | Genuine Human and isolated Learning decision evidence traverses the completed boundaries without introducing account, authentication, or permission semantics; `tests/slice3-local-landing.test.sh`; `evidence/release-gate/own-003-durable-decision-inventory.md`, `evidence/slice-4/s4-001-human-execution.md`, `evidence/slice-5/s5-006-test-captain-decisions.md` |
| INIT-001 | complete | `tests/gate0-tracking.test.sh` validates the frozen repository/branch/commit/tree, import method/time, license digest, and attribution without network access | `firstmate.provenance.json`, `firstmate/LICENSE`, `evidence/gate-0/provenance.md` |
| INIT-002 | complete | Provenance source commit differs from and supersedes research SHA | `evidence/gate-0/provenance.md` |
| INIT-003 | complete | `tests/init.test.sh` covers explicit/repeated init, all seven supported operational directories, writeability checks, registry, built-in unarchivable Escalation Clerk, and init-owned repair validation; isolated missing-Node, missing-Git, malformed complete-provenance, and license-digest fixtures all refuse before state mutation | `evidence/release-gate/rel-001-clean-init.md` |
| INIT-004 | complete | `tests/gate0-primary-launch.test.sh` invokes both public `primary --tui` and `web` launch paths against isolated uninitialized state, requires an explicit initialization error from each, and proves neither path creates the initialization state marker | `evidence/release-gate/rel-001-clean-init.md`, `evidence/gate-0/init.md` |
| INIT-005 | complete | `tests/init.test.sh` covers all seven unmarked operational roots, unsupported versions, conflicting init-owned state, nonconforming path types, and current-version repairs; every refusal snapshots paths and bytes before and after | `evidence/gate-0/init.md` |
| G0-001 | complete | `tests/init.test.sh` passes clean and byte-idempotent repeated init plus the isolated 18-case unknown/conflict/repair matrix | `evidence/gate-0/init.md` |
| G0-002 | complete | `tests/gate0-tracking.test.sh` validates root tracking, nested-Git/submodule/subtree/patch-stack rejection, provenance invariants, and runtime exclusion | `evidence/gate-0/provenance.md` |
| G0-003 | complete | `tests/gate0-primary-launch.test.sh` catches TUI/Web argv, environment, cwd, and extension divergence; focused Firstmate lock/session-start regressions remain green | `G0_003_LIVE=1 bash tests/cert/g0-003-real-web-tui.sh` passed both real Pi 0.82.0 winner orders; `evidence/gate-0/lock-liveness.md`, `evidence/gate-0/artifacts/g0-003-*` |
| G0-004 | complete | `tests/gate0-no-forge.test.sh` isolates `HOME`/Git config, restricts `PATH`, uses forge executables only as failing invocation tripwires, and drives real session-start, Project preflight, local Git, and spawn-refusal paths | `evidence/gate-0/no-forge.md` |
| PROJ-003 | complete | The G0-004 test proves ordinary startup makes no forge readiness probe and dispatch preflight is mode-specific; `test:slice3-project-catalog` proves both Project addition and mode switching run the target mode's read-only preflight against staged authority before atomic publication, leave the registry byte-identical on refusal, and clean up only a newly cloned Project after failed add readiness | The guarded S3-007 run exercised both production `direct-PR` and `no-mistakes` preflights with real tools and authentication against only the Captain-authorized isolated private fixture; `evidence/slice-3/s3-007-remote-certification-blocker.md`, `evidence/slice-3/artifacts/s3-007-remote-foundation.txt` |
| PROJ-004 | complete | `fm-spawn.sh` calls `fm-project-preflight.sh`; G0-004 proves a mode-specific auth refusal occurs before brief, worktree, endpoint, Worker, or `.meta` creation and leaves an unrelated local-only Task dispatchable. The production Primary Extension requires the exact same command before compile/spawn and hard-stops on its failure; its contract test freezes both the shared-command identity and zero-mutation boundary | A genuine S3-001 dispatch exercised the production `fm-spawn.sh` preflight before allocating its Treehouse worktree and Worker, while genuine CERT-002/S2-003 runs prove the canonical Pi Primary loads and follows the production Extension command protocol rather than an alternate dispatch implementation; `evidence/gate-0/no-forge.md`, `evidence/slice-3/s3-001-local-delivery.md`, `evidence/slice-1/s1-003-pi-stream-extension-ui.md`, `evidence/slice-2/s2-003-primary-selection.md` |
| COMP-001 | complete | `test:gate0-tracking` pins the sole supported Firstmate compatibility baseline to the exact repository, branch, source commit, source tree, import commit, and vendored path in `firstmate.provenance.json`; the product ships and tests that directly tracked tree and exposes no upstream-version selector, migration command, compatibility adapter, or claim for any other Firstmate commit | `evidence/gate-0/provenance.md`; the complete vendored baseline is exercised by `test:firstmate` and recorded in `evidence/gate-0/firstmate-baseline.md` |
| COMP-003 | complete | Published V1 Web contracts retain distinct versioned schema files and reproducibly generated types; the runtime-validated `clerkmesh.api-capabilities.v1` production endpoint advertises the exact supported model versions so future versions can coexist and callers can negotiate instead of receiving a destructive silent replacement | `node tests/api-capabilities.test.mjs`; `@clerkmesh/shared check:generated`; compatibility and version-addition rule: `evidence/release-gate/comp-003-schema-compatibility.md` |
| COMP-002 | complete | The unmodified vendored regression suite covers TUI session startup and Firstmate lock arbitration, Worker spawn/supervision, fleet synchronization and snapshots, bearings snapshots, no-mistakes ownership/workflow, and fail-closed teardown. ClerkMesh integration checks additionally preserve the shared TUI/Web Primary lock, mode-specific local-only/direct-PR/no-mistakes dispatch contracts, exact reviewed-tip local landing, remote landing activity, and teardown refusal/preservation behavior | The complete 96-script Firstmate baseline passes with no waived failures (`evidence/gate-0/firstmate-baseline.md`); genuine Web/TUI competition, Pi/Herdr/Treehouse Worker lifecycle, local landing/teardown, and isolated remote direct-PR/no-mistakes deliveries pass in `evidence/gate-0/lock-liveness.md`, `evidence/slice-3/s3-real-herdr-treehouse-foundation.md`, `evidence/slice-3/s3-001-local-delivery.md`, and `evidence/slice-3/s3-007-remote-certification-blocker.md` |
| COMP-004 | complete | `tests/init.test.sh` treats existing content in every supported operational root as unknown unless it carries the exact V1 ownership marker; refuses expected-looking partial output, unsupported or malformed versions, conflicting current-version authority, symlink layouts, and modified built-in state without mutation; and permits repair only for missing init-owned pieces under an exact current marker. `tests/gate0-primary-launch.test.sh` proves neither public launch path adopts or lazily initializes unrecognized state | The isolated 22-case clean-root state matrix is recorded in `evidence/release-gate/rel-001-clean-init.md`; ClerkMesh exposes no legacy Firstmate Home import, prerelease-format migration, or hand-built-layout adoption path |
| COMP-005 | complete | G0-004 proves startup and local-only preflight need no forge account/auth while explicit remote modes fail closed; the genuine S3-001 lifecycle completes without a remote, `gh`, or forge auth | `evidence/slice-3/s3-001-local-delivery.md` |
| G0-005 | complete | `corepack pnpm run test:firstmate` passes all 96 upstream scripts with 0 failures and 15 declared gate skips; no failure was waived | `evidence/gate-0/firstmate-baseline.md`, focused fixture evidence, and the historical diagnostic artifact at `evidence/gate-0/artifacts/firstmate-baseline.json` |
| PROC-001 | complete | `tests/gate0-primary-launch.test.sh` proves `bin/clerkmesh web` directly launches exactly one composed foreground product application; `test:slice1-schema` proves the application has no daemon, supervisor-restart, or Primary auto-restart path and transitions an exited Primary offline | Genuine G0-003 Web/TUI competition exercises the foreground Web owner and its single Pi child; S1-006 externally terminates a genuine Pi Primary, observes offline, refuses later mutation, and proves no replacement PID appears. `evidence/gate-0/lock-liveness.md`; `evidence/slice-1/s1-006-shutdown-offline.md` |
| PROC-002 | complete | `test:slice1-schema` proves the composed product application binds Fastify close to exactly one owned-child SIGTERM, shares idempotent SIGINT/SIGTERM cleanup, and closes on listener failure without any Worker ownership dependency; browser WebSocket disconnects alter only process-local lease/subscription state and never close Web or its Primary. `test:slice3-project-catalog` additionally proves unexpected Primary exit leaves Worker polling active, Web exit stops only process-local polling, and Web restart recovers external Worker facts without inventing a Primary or completion | `cert:slice1-shutdown-offline` proves Web shutdown terminates its genuine Pi child while an independently live Worker survives. Genuine Pi/Herdr S5-002 and S5-005 runs prove Learning Agents continue after both normal launch-owner return and abrupt `SIGKILL`, with later production reconciliation from durable authority; `evidence/slice-1/s1-006-shutdown-offline.md`, `evidence/certifications/cert-005-learning-continuity.md` |
| PROC-003 | complete | `tests/gate0-primary-launch.test.sh` proves Web and TUI derive the same canonical `FM_HOME`, run from the same Firstmate cwd, load the identical explicit Firstmate and ClerkMesh Primary Extension set, and delegate single-writer authority to Firstmate's shared lock; the genuine G0-003 competition verifies both public launch modes contend on that lock without divergent homes or extension baselines | Genuine CERT-002 starts the canonical Web/RPC Primary with the production ClerkMesh Primary Extension, requires its command discovery, observes its extension UI and streamed model run, and verifies complete once-per-run protocol injection; `evidence/gate-0/lock-liveness.md`, `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| CONV-001 | complete | `test:slice1-schema` freezes the opaque, path-free session catalog boundary and verifies server discovery uses the pinned Pi 0.82.0 `SessionManager.listAll` metadata API, canonicalizes cwd/symlink aliases, omits foreign/invalid sessions, fails closed on duplicate IDs, and serves the Ajv-validated projection over Fastify without a Primary launch dependency; the production React client build browses this catalog and retains only the opaque selection in URL state, while the guarded product Fastify process serves the built shell and containment-checked immutable assets | `cert:slice1-history-browse` parses an isolated persisted Captain history through real Pi 0.82.0 metadata discovery, filters foreign cwd through Fastify, and proves the Primary/provider tripwire remains untouched; `evidence/slice-1/s1-001-history-browse.md` |
| CONV-002 | complete | `test:slice1-schema` proves concurrent HTTP first sends share one startup promise, resolve opaque session IDs server-side, reject path-bearing mutation input, lock selection once startup begins, and never prompt before startup succeeds; before send, the client keeps selected history solely in browser URL state | `cert:slice1-concurrent-first-send` concurrently replays one request through a genuine Pi 0.82.0 RPC child and proves exactly one visible Captain message, one model reply, and `agent_settled`; `evidence/slice-1/s1-002-concurrent-first-send.md` |
| CONV-003 | complete | The product application composes one shared supervisor/projection/idempotency map/lease, keeps catalog queries launch-free, stops its owned RPC child on Fastify close, projects child exit as offline, refuses subsequent startup/prompt attempts without auto-restart, and locks selection after startup begins | `cert:slice1-shutdown-offline` externally terminates a genuine Pi child, observes offline, proves a later mutation returns 503, and verifies no replacement PID appears; `evidence/slice-1/s1-006-shutdown-offline.md` |
| CONV-005 | complete | `test:slice1-schema` proves one WebSocket carries lease state, reply/projection events, extension UI, periodic heartbeats, and Work projection updates while HTTP remains the query/mutation transport; the production client consumes that socket and sends mutations over HTTP; no SSE transport exists | `cert:slice1-stream-extension-ui` proves genuine Pi reply streaming and extension UI enter the shared projection; `cert:slice1-refresh-restart` proves a new browser-equivalent WebSocket receives pending genuine extension UI in its initial snapshot after refresh; `evidence/slice-1/s1-003-pi-stream-extension-ui.md`, `evidence/slice-1/s1-005-refresh-restart.md` |
| CONV-004 | complete | `test:slice1-schema` binds normalized conversation events to Pi 0.82 RPC event shapes without parsing terminal/ANSI output, proves strict bounded JSONL command correlation/event forwarding, and integrates that client and normalizer through the single-child product supervisor | `cert:slice1-stream-extension-ui` proves genuine Pi RPC visible stream, durable reply, extension UI, and `agent_settled`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| CONV-006 | complete | `test:slice1-schema` proves process-local `(client token, requestId)` HTTP replay returns the original result without a duplicate prompt, while conflicting reuse returns 409 | `cert:slice1-reconnect-idempotency` proves concurrent replay and conflict handling over real loopback HTTP; the guarantee is explicitly process-local and ends at restart; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| CONV-007 | complete | `test:slice1-schema` uses fake-clock unit and real loopback WebSocket/HTTP integration to prove first-token acquisition, multiple same-token connections, a strict three-second reconnect reservation, second-token HTTP 409 refusal, explicit claim after expiry, and vacant new-connection acquisition; the production client uses a stable opaque browser token, displays lease state, disables read-only mutation, and exposes explicit claim | `cert:slice1-reconnect-idempotency` uses browser-equivalent real WebSocket/HTTP connections and real wall-clock expiry to prove same-token reconnect, second-token refusal, post-expiry explicit claim, and old-owner refusal; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| CONV-008 | complete | The WebSocket lease accepts opaque local client tokens only and exposes no actor/authentication semantics; HTTP mutation uses the token solely to check writability; the production client labels state and claims only as a local write lease and explicitly says it is not authentication or identity | `test:slice1-schema` checks both source semantics and the built production client |
| CONV-009 | complete | `test:slice1-schema` proves a versioned, schema-valid process-local projection retains at most 10,000 normalized events, assigns monotonic sequence cursors, retains pending extension UI, and sends an initial runtime-validated WebSocket snapshot followed by live monotonic continuations without exposing diagnostics by default; selected-session startup rebuilds only durable user/assistant visible messages from Pi `get_messages` and does not synthesize tool results, reasoning, stream fragments, statuses, or diagnostics; the production client deduplicates snapshots by sequence and retains the bounded visible timeline | `cert:slice1-refresh-restart` proves a browser-equivalent refresh receives genuine Pi extension UI from the live process snapshot, then a new Web process reconstructs durable Captain history through genuine Pi RPC without inventing prior extension UI, stream fragments, or diagnostics; `evidence/slice-1/s1-005-refresh-restart.md` |
| CONV-010 | complete | `test:slice1-schema` proves ordinary snapshots exclude diagnostic events while explicit diagnostic snapshots include them, with defensive payload copies; Pi 0.82 RPC fixtures project visible text/stream/status/extension UI, keep reasoning/tool lifecycles diagnostic-only, redact credential-shaped fields, whole environment maps, and Bearer values before storage, and require extension command discovery before prompt acceptance; the production client keeps diagnostics off by default and requires an explicit warning-bearing opt-in before subscribing to and rendering redacted diagnostic detail; the real Primary Extension registers the required command and injects its complete tracked behavior protocol exactly once per run | A genuine Pi tool lifecycle traverses the real RPC normalizer into the opt-in diagnostic projection, remains absent from the ordinary projection, and retains no real `HOME` path; `S1_003_LIVE=1 corepack pnpm run cert:slice1-stream-extension-ui`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| EXEC-001 | complete | `test:slice1-schema` proves the Primary Extension appends the complete tracked `CLERK.md` exactly once on each `before_agent_start` run and registers `/clerkmesh-status`; the protocol names the cwd-independent inspect/index/shortlist and context-compile control-plane calls, requires their actual execution, and stops before spawn/readiness on command failure while explicitly retaining selection, brief, spawn, and assignment authority outside the extension; `tests/gate0-primary-launch.test.sh` proves both canonical Primary modes load it | `cert:slice1-stream-extension-ui` proves the canonical genuine Pi child loads and executes the extension before a real settled agent run |
| EXEC-002 | complete | The strict RPC client rejects prompting unless extension-sourced `/clerkmesh-status` is discovered, the extension contract proves the matching command is registered, and the canonical Web/RPC launch includes that extension | `cert:slice1-stream-extension-ui` starts genuine Pi, passes command discovery, and invokes the command's real UI notification |
| EXEC-003 | complete | The tracked behavior protocol and extension implementation contain no selection, input modification, spawn interception, brief write, or assignment state; the real Firstmate Pi Worker launch contract loads only its task-scoped turn-end extension and excludes the ClerkMesh Primary Extension and its command | `cert:slice1-stream-extension-ui` proves the extension behavior at the canonical Primary boundary; Worker exclusion is verified at Firstmate's concrete launch-command boundary |
| CONV-011 | complete | `tests/gate0-primary-launch.test.sh` proves both Web/RPC and TUI launch the same canonical Pi/Firstmate Extension stack without a ClerkMesh lock preflight or alternate lock path; `tests/gate0-lock.test.sh` freezes Firstmate's sole live-owner refusal and dead-owner reclamation behavior | Genuine G0-003 competition ran both winner orders through the public launch paths: Firstmate itself admitted one Primary, made the second read-only without lock overwrite or loser-side mutation, and cleaned up the Web-owned child; `evidence/gate-0/lock-liveness.md`, `evidence/gate-0/artifacts/g0-003-real-web-tui.txt` |
| UI-001 | complete | The production React build provides the fixed Conversations, Work, Company, and Reviews calm object workspaces; each focuses a persistent object with a breadcrumb, including individual Proposal/target review selection | `test:slice5-learning` verifies the Reviews source and production bundle; existing Slice 1–3 client checks cover the other workspaces |
| UI-002 | complete | Navigation exposes only implemented production-backed workspaces: Reviews was added only with its authoritative query and complete interface, while Company has explicit loading/error/freshness states rather than shells or fake data | `test:slice5-learning` verifies the Reviews workspace and compiled production asset |
| UI-003 | complete | Work composes its graph, running list, and selected Task detail from the authoritative Firstmate Task projection plus Herdr runtime projection; its only change affordance creates a prefilled, editable message in the currently selected conversation rather than mutating Task authority directly | `test:slice3-project-catalog` verifies shared projection consumption, Task detail routing, editable draft creation, conversation navigation, and the compiled production client |
| UI-004 | complete | Company presents Project and Clerk as distinct production-backed object views; Reviews queries the path-free authoritative catalog and groups targets by Proposal while displaying Source preview/hash, changed paths, full diff, base/tree identity, warnings, validation, and decisions | `test:slice5-learning` proves the fail-closed HTTP projection, private-path exclusion, interface semantics, and compiled production Reviews workspace |
| SEC-003 | complete | RPC normalization redacts credential-shaped fields, whole environment maps, Bearer values, `HOME`, and values of credential-shaped process environment keys before projection; the production diagnostic view is opt-in, visibly warns about sensitive operational detail, and ordinary mode receives no diagnostic events. Focused tests cover structured and string-embedded secrets | A genuine Pi Bash lifecycle prints planted environment-token and Authorization-header values through real tool arguments/output; the opt-in projection retains redaction markers but neither planted value nor real `HOME`, while ordinary projection retains no diagnostics; `S1_003_LIVE=1 corepack pnpm run cert:slice1-stream-extension-ui`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| SEC-005 | complete | The injected Primary protocol defines `yolo` only as bounded delivery automation and requires explicit Captain confirmation for destructive, security-sensitive, irreversible, or out-of-request decisions; it also prohibits bypassing Firstmate's cleanliness, exact-tip, and fast-forward landing safeguards. The local landing matrix proves yolo-off approval refusal and yolo-on divergence refusal | `S3_002_LIVE=1 corepack pnpm run cert:slice3-yolo-escalation` passed with a genuine Pi 0.82.0 Primary that awaited explicit Captain confirmation and did not reach the destructive credential/data tripwire; `evidence/slice-3/s3-002-yolo-escalation.md` |
| SEC-001 | complete | `bin/clerkmesh web` binds the production entrypoint to the guarded listener; `test:slice1-schema` proves it defaults to `127.0.0.1`, warns conspicuously before a deliberate non-loopback bind, and every conversation route rejects non-loopback or malformed Host, forwarded Host, DNS-rebinding authorities, and non-loopback/null Origin before handlers; HTTP mutations separately require JSON Content-Type | `cert:production-browser-security` drives browser-equivalent requests through the real production listener, verifies the safe default and guarded request boundary, and observes the conspicuous warning from a deliberate non-loopback production bind; `evidence/certifications/sec-001-production-browser-security.md` |
| SEC-002 | complete | Gate 0 proves startup and local-only delivery require no forge authentication; Slice 1 freezes the browser token as a process-local write lease rather than authentication and exposes no credential-entry API; Slice 3 delegates remote readiness and transport to Git/SSH and the selected forge CLI without adding credential fields to ClerkMesh authority. A source inventory finds no ClerkMesh secret input, credential store, token exchange, or authentication facility | Genuine Pi, Herdr, and guarded remote runs consume configuration from their owning external tools without copying it into ClerkMesh; `evidence/release-gate/sec-002-credential-ownership.md`, `evidence/slice-3/s3-007-remote-certification-blocker.md` |
| SEC-004 | complete | The production client globally discloses that general shell tools run as the launching OS user and ClerkMesh is not a confidentiality or tamper-resistant sandbox; the client semantics test requires that language in both source and the built asset. `SECURITY.md` defines filesystem/repository/process containment and review controls as mistake-reduction rather than hard isolation and directs stronger-boundary users to an externally managed OS account, VM, or sandbox | `evidence/release-gate/sec-004-same-user-boundary.md` |
| READ-003 | complete | Every V1 model that crosses the Web boundary has a versioned schema in `packages/shared/schemas/` and a reproducibly generated TypeScript interface: conversation sessions/events, Clerk catalog, Firstmate Project/Task/Herdr/activity projections, transient Task detail, and Learning list/proposal. Production HTTP query routes compile and enforce the applicable Ajv schemas before publication; the shared WebSocket path runtime-validates conversation snapshots and schema-valid Task/Herdr poll results before forwarding them. `fm-task-activity.v1` is schema/type/projector validated but intentionally has no Web route because V1 does not consume it across that boundary; `clerkmesh.execution-context.v1` likewise remains an internal machine-block contract rather than a Web payload. | `test:slice1-schema`, `test:slice2-clerk-repository`, `test:slice3-project-catalog`, and `test:slice5-learning` exercise the runtime boundaries; `@clerkmesh/shared check:generated` rejects stale generated types |
| CERT-001 | complete | Opt-in real runner uses Pi 0.82.0, real tmux/TUI, and real Pi RPC through the ClerkMesh Web process path | `evidence/gate-0/artifacts/g0-003-real-web-tui.txt` |
| S1-003 | complete | The opt-in runner starts one genuine Pi RPC Primary and requires extension command discovery, extension UI, non-empty visible stream, durable reply, and `agent_settled` | `S1_003_LIVE=1 corepack pnpm run cert:slice1-stream-extension-ui`; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| S1-004 | complete | Contract tests cover lease edge cases and the tracked certification drives browser-equivalent real loopback WebSocket and HTTP transports with wall-clock reconnect expiry | `corepack pnpm run cert:slice1-reconnect-idempotency`; `evidence/slice-1/s1-004-reconnect-idempotency.md` |
| S1-005 | complete | Projection and reconstruction contracts are exercised by `test:slice1-schema`; the tracked certification uses genuine Pi RPC and browser-equivalent WebSocket refresh | `corepack pnpm run cert:slice1-refresh-restart`; `evidence/slice-1/s1-005-refresh-restart.md` |
| S1-006 | complete | Supervisor and application contracts prove owned-child cleanup, Worker-independent shutdown, offline projection, and no auto-restart | `corepack pnpm run cert:slice1-shutdown-offline` uses genuine Pi children and an independent live Worker ownership tripwire; `evidence/slice-1/s1-006-shutdown-offline.md` |
| CERT-002 | complete | Contract tests verify complete once-per-run protocol injection; the opt-in real runner verifies canonical extension loading, command discovery, extension UI, Pi RPC stream, and settled model execution | `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| CERT-003 | complete | Isolated genuine Herdr/Treehouse runs cover Worker spawn, status projection, durable Captain-relevant wake and drain, genuine Pi follow-up, completion, and Web-exit recovery | `evidence/certifications/cert-003-worker-runtime.md` |
| CERT-004 | complete | The isolated genuine Pi/Herdr/Treehouse run uses real Git for local-only worktree execution, validation, authoritative review, guarded fast-forward landing, landed proof, and teardown | `evidence/slice-3/s3-001-local-delivery.md` |
| CERT-005 | complete | Genuine Pi/Herdr runs prove two-target extraction continues after normal and SIGKILL Primary-owner exits, then a separate production invocation reconciles solely from durable Proposal authority without relaunching Agents or inventing decisions | `evidence/certifications/cert-005-learning-continuity.md`; `evidence/slice-5/s5-002-real-learning-extraction.md`; `evidence/slice-5/s5-005-restart-reconciliation.md` |
| CERT-006 | complete | Guarded genuine runs against the Captain-authorized private fixture exercised production direct-PR and no-mistakes preflights, branch-specific PR creation, immutable head/ancestry verification, complete-diff review, and squash merge | `evidence/slice-3/s3-007-remote-certification-blocker.md`; `evidence/slice-3/artifacts/s3-007-real-direct-pr.txt`; `evidence/slice-3/artifacts/s3-007-real-no-mistakes.txt` |
| CLERK-001 | complete | `test:slice2-clerk-repository` freezes and verifies the strict repository contract: every ordinary Clerk is an independent real-Git repository under canonical `clerks/<name>` containment; `CLERK.md` has exact frontmatter and the ordered six-section schema; root layout, file and aggregate size limits, indexed material metadata, Skill structure, and symlink/nested-repository refusal all fail closed. The create/register/validate/inspect/review/commit/archive/restore lifecycle tests exercise that same contract rather than a parallel fixture format | The S2-001 isolated lifecycle creates, validates, registers, reviews, commits, archives, restores, and recovers a conforming independent Clerk repository through production commands; genuine S2-003 then selects and compiles an approved Clerk from the same repository boundary; `evidence/slice-2/s2-001-clerk-lifecycle.md`, `evidence/slice-2/s2-003-primary-selection.md` |
| CLERK-002 | complete | `test:slice2-clerk-repository` and its focused CLI checks validate only the registry-pinned real-Git commit snapshot; prove dirty and untracked working-tree content cannot enter validation, inspection, review, catalog projection, compilation, or bounded retrieval; reject unavailable historical object IDs instead of falling back to HEAD; and fail closed on HEAD drift before context publication. The compiler records the exact selected commit and blob OIDs in the integrity-protected execution context, and the capability reader resolves only those immutable Git objects | Genuine S2-003 selected and compiled the registry-approved commit through the production Primary lifecycle; genuine S2-004 retrieved only its allowlisted immutable blob and refused excluded content; genuine S2-005 retained that commit and capability across follow-up while a new execution rotated to a distinct approved commit; `evidence/slice-2/s2-003-primary-selection.md`, `evidence/slice-2/s2-004-worker-capability.md`, `evidence/slice-2/s2-005-worker-continuity.md` |
| CLERK-003 | complete | `test:slice2-clerk-repository` freezes the strict registry v1 table parser/renderer and its only four fields (name, canonical path, `active|archived`, built-in), canonical `clerks/<name>` containment, unique names, and built-in Escalation invariants. The production create/register/archive/restore lifecycle mutates that single registry through validated atomic publication; archive preserves the repository and registry identity, restore reactivates that same identity, and both create and register refuse every previously registered name—including archived names—without changing source, repository, or registry authority | The isolated S2-001 production lifecycle created, registered, archived, and restored ordinary Clerks through the same registry boundary, with kill/restart recovery preserving the original name and repository identity; `evidence/slice-2/s2-001-clerk-lifecycle.md` |
| CLERK-004 | complete | Registry and lifecycle contracts keep the built-in human Escalation Clerk active, unique, and unarchivable; the Primary protocol prohibits automatic selection | `S2_007_LIVE=1 corepack pnpm run cert:slice2-clarification-escalation` passed with a genuine Pi Primary and selected Escalation only after explicit Captain takeover; `evidence/slice-2/s2-007-clarification-escalation.md` |
| CLERK-005 | complete | `test:slice2-clerk-repository` rejects executable material outside Skills and every executable Skill auxiliary file not explicitly referenced by its `SKILL.md`; the review/commit lifecycle validates the immutable reviewed candidate tree and advances the approved Git commit only by compare-and-swap, so referenced scripts become approved capability only through Captain review and commit. Execution-context schema/compiler/reader tests exclude scripts and Sources from the Worker material allowlist, bind readable material to immutable blobs, and expose only bounded `list/search/read`, so Clerk content cannot enlarge Worker permissions | The genuine S2-004 Herdr/Pi Worker could use only its immutable allowlisted read capability and was refused access to a Skill script, Source, and traversal path; `evidence/slice-2/s2-001-clerk-lifecycle.md`, `evidence/slice-2/s2-004-worker-capability.md` |
| LIFE-001 | complete | `test:slice2-clerk-repository` exercises all eight narrow Agent-facing create/register/validate/inspect/review/commit/archive/restore commands without a dispatcher | Full lifecycle integration remains |
| LIFE-002 | complete | Every lifecycle command emits only compact tab, Markdown, or requested diff output on success, diagnostics only on stderr, and standard zero/nonzero status; failures emit no stdout | CLI output contract is covered |
| LIFE-003 | complete | `test:slice2-clerk-repository` proves the shared lifecycle mutation lock refuses a live PID, reclaims a dead owner, fails closed on malformed ownership, and ownership-checks release; every lifecycle mutation, including commit, binds to that authority | A real forcibly killed archive mutation leaves a stale process lock that restart safely reclaims; repeated at the Slice 3 boundary in `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| LIFE-004 | complete | `test:slice2-clerk-repository` proves registry replacement uses validated atomic rename; create/register stage repositories safely; creation and registration journal the repository-to-registry publication gap; archive and restore journal intended registry transitions before publication; all four deterministically complete interrupted publication on restart; malformed journal state fails closed; and commit validates both immutable endpoints and the reviewed candidate tree before a Git `update-ref` compare-and-swap, refusing tree mismatch and HEAD races without moving the ref | A real `SIGKILL` between durable intent and completion is recovered by the next locked lifecycle invocation; the archive/restore/CAS/recovery boundary was repeated for S3-006 in `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| S2-001 | complete | `test:slice2-clerk-repository` verifies repository and registry contracts against isolated fixtures and real Git: create/validate/register produce valid independent repositories, dirty/untracked source content is excluded, interrupted publication recovers, immutable review and CAS commit remain fixed-tree, and archive/restore plus live-lock refusal pass | Real kill/restart recovery evidence: `evidence/slice-2/s2-001-clerk-lifecycle.md` |
| S2-002 | complete | `test:slice1-schema` proves complete once-per-run Primary injection and canonical launch loading; `test:slice2-clerk-repository` additionally drives Firstmate's concrete Pi Worker spawn and proves its argv loads only the task-scoped Worker extension, never the ClerkMesh Primary Extension or `/clerkmesh-status` | Existing real Pi `cert:slice1-stream-extension-ui` evidence proves canonical Primary extension loading, command discovery, complete injection, and a settled run; `evidence/slice-1/s1-003-pi-stream-extension-ui.md` |
| S2-003 | complete | Frozen schema, immutable compiler, atomic brief integration, narrow commands, and fail-closed injected protocol pass in `test:slice2-clerk-repository`; the opt-in runner independently validates the resulting payload | `S2_003_LIVE=1 corepack pnpm run cert:slice2-primary-selection` passed with genuine Pi 0.82.0 semantic selection and compilation; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-004 | complete | The injected protocol requires candidates → shortlist → semantic selection → compile through the narrow immutable-commit commands and forbids descriptive substitution; the isolated certification presents competing active Agent Clerks | A genuine Pi 0.82.0 Primary selected the semantically appropriate review Clerk and compiled its context; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-007 | complete | `test:slice2-clerk-repository` freezes the strict `clerkmesh.execution-context.v1` payload with Task ID, Clerk name/execution/full commit, identity context, selection reason/boundaries, and approved material path/blob allowlist; generated TypeScript freshness is checked; the compiler binds identity and Primary-explicitly-selected material blob OIDs to an immutable approved commit, refuses omitted, duplicate, unsupported, or absent material selections and HEAD drift, excludes Sources and non-readable paths, emits deterministic canonical JSON/base64/SHA-256, and atomically inserts one standard replaceable machine block into an existing brief; a cwd-independent Primary-facing command exposes that exact pipeline with compact identity/hash output and failure-without-mutation coverage | `cert:slice2-primary-selection` independently verified the genuine Primary's canonical payload hash, Task, selected Clerk, approved commit, and explicit blob allowlist; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-008 | complete | Compiler and atomic brief integration tests require the selected Clerk HEAD to remain the approved commit immediately before publication, preserve existing brief prose, and copy no material body or assignment file | The real S2-003 run compiled the standard machine block into an existing isolated brief and independently matched its commit and hash; `evidence/slice-2/s2-003-primary-selection.md` |
| EXEC-005 | complete | The injected protocol requires ambiguous, unsafe-to-split, and unmatched requests to remain in conversation for clarification and forbids automatic Escalation selection | A genuine Pi Primary inspected candidates, left an unsafe underspecified request in conversation without compiling a brief, and selected Escalation only after explicit Captain takeover; `evidence/slice-2/s2-007-clarification-escalation.md` |
| EXEC-006 | complete | The injected protocol defaults to automatic planning but requires a requested split/selection/reason/boundary preview to wait for Captain response | The genuine S2-007 run presented requested handling, Clerk, reason, and boundary information, then waited without brief mutation until the next Captain turn; `evidence/slice-2/s2-007-clarification-escalation.md` |
| EXEC-009 | complete | `test:slice2-clerk-repository` proves a current Worker capability remains fixed to its original brief hash, commit, and blob allowlist across follow-up use, while a newly published execution context creates a distinct capability and cannot access the prior allowlist; `cert:slice2-worker-continuity` exercises the same boundary with genuine Workers | A genuine Pi session retained the old immutable capability across its initial turn and follow-up, while a distinct Herdr/Pi Worker used the rotated context and refused the old path; `evidence/slice-2/s2-005-worker-continuity.md` |
| EXEC-010 | complete | `test:slice2-clerk-repository` fail-closed parses exactly one canonical brief machine block, verifies its canonical base64/SHA-256 integrity and strict Agent context, rejects duplicate entries, Sources, scripts, unsupported paths and traversal, and exposes bounded Worker-facing `list/search/read` only against allowlisted immutable Git blobs while excluding dirty working-tree content; the injected Primary protocol requires the existing brief to give Workers the concrete bounded command before ordinary spawn and forbids copied material bodies or parallel context/assignment records | A genuine Herdr/Pi Worker listed and read only the immutable allowlisted blob while Source, script, and traversal reads were refused; `evidence/slice-2/s2-004-worker-capability.md` |
| EXEC-011 | complete | The cwd-independent Worker capability command derives its repository only beneath the canonical Clerk root and writes content-free JSONL audit records for allowed and refused operations beneath the canonical state root in `capabilities/<context-sha256>.jsonl`; tests prove audit contains context identity but no dirty material and refuse relative, aliased, or symlinked Clerk/state/capability roots; the Primary protocol names this command as the sole Worker material interface while keeping capability state out of the brief | The genuine Worker run independently verified allowed/refused content-free audit records under the canonical state root without leaking refused content; `evidence/slice-2/s2-004-worker-capability.md` |
| EXEC-012 | complete | Agent Clerk selection changes only the integrity-checked execution-context block in the ordinary Firstmate brief: `test:slice2-clerk-repository` exercises the existing `fm-spawn.sh` Worker command and task-scoped extension unchanged, immutable follow-up reuse, and bounded Clerk material access without introducing a Clerk runtime, endpoint, watcher, or retry branch. `test:slice3-project-catalog` exercises the same Firstmate status, wake, validation, review, landing, and teardown paths for Clerk-context-bearing Agent Tasks | Genuine S2-004/S2-005 Workers used ordinary Herdr/Pi spawn and follow-up, CERT-003 traversed the production Firstmate status/watcher/wake path, and the genuine S3-001 lifecycle used the ordinary Firstmate local delivery path through validation, review, explicit approval, exact-tip landing, and teardown; `evidence/slice-2/s2-004-worker-capability.md`, `evidence/slice-2/s2-005-worker-continuity.md`, `evidence/certifications/cert-003-worker-runtime.md`, `evidence/slice-3/s3-001-local-delivery.md` |
| S2-004 | complete | Canonical reader, immutable capability operations, bounded Worker command, audit containment, Primary brief instructions, and the concrete Firstmate Worker extension boundary pass in `test:slice2-clerk-repository` | `S2_004_LIVE=1 corepack pnpm run cert:slice2-worker-capability` passed with a genuine Herdr 0.7.4/Pi 0.82.0 Worker; `evidence/slice-2/s2-004-worker-capability.md` |
| S2-005 | complete | `test:slice2-clerk-repository` covers immutable follow-up capability retention and new-execution hash/commit/allowlist rotation without cross-snapshot access; the opt-in runner drives two turns in one Pi session and a distinct new Herdr/Pi Worker | `S2_005_LIVE=1 corepack pnpm run cert:slice2-worker-continuity` passed with genuine Herdr 0.7.4/Pi 0.82.0 Workers; `evidence/slice-2/s2-005-worker-continuity.md` |
| S2-006 | complete | `cert:slice2-task-detail` verifies schema-valid path-free Clerk catalog and Firstmate Task graph projections, guarded HTTP validation, integrity-checked transient Task-detail composition, nullable fail-closed enrichment, and absence of Clerk ownership fields from Firstmate's production control plane | `evidence/slice-2/s2-006-task-detail-projection.md` |
| S2-007 | complete | Extension and repository contracts enforce clarification, preview waiting, and explicit-only Escalation selection; the opt-in runner verifies all three turns and byte-identical brief retention | `S2_007_LIVE=1 corepack pnpm run cert:slice2-clarification-escalation` passed with a genuine Pi Primary; `evidence/slice-2/s2-007-clarification-escalation.md` |
| LIFE-005 | complete | `clerk-catalog.v1` freezes a path-free Web boundary distinct from registry Markdown, with generated TypeScript freshness checks; the server projector consumes only the canonical registry parser and the guarded `/api/clerks` query runtime-validates its path-free approved-commit projection | Focused stage evidence: `evidence/slice-2/s2-006-task-detail-projection.md` |
| READ-002 | complete | All four required ClerkMesh models—`clerk-catalog.v1`, `clerkmesh.execution-context.v1`, `learning-list.v1`, and `learning-proposal.v1`—have versioned schemas in `packages/shared/schemas/` and reproducibly generated TypeScript interfaces. The production Learning review projector emits the two Learning models with explicit provenance/freshness and the guarded HTTP endpoint performs Ajv runtime validation before publication | `test:slice2-clerk-repository` covers Clerk catalog and execution context; `test:slice5-learning` covers the path-free Learning list/proposal projection and HTTP boundary |
| READ-004 | complete | Every V1 Web projection (`conversation-sessions.v1`, `conversation-events.v1`, `clerk-catalog.v1`, `fm-project-catalog.v1`, `fm-task-graph.v1`, `clerkmesh-task-detail.v1`, `fm-herdr-agents.v1`, `learning-list.v1`, and nested `learning-proposal.v1`) explicitly declares schema, observation time, `current|stale|unknown` freshness as applicable, authority provenance, omissions, and errors. Focused projection tests prove malformed, unavailable, filtered, stale, and bounded-window facts become explicit metadata rather than silently disappearing; Learning targets without reviews are reported at both catalog and Proposal scope. The production Web client consumes only schema-validated HTTP/WebSocket projections and contains no parser for backlog, brief/report prose, `.meta`, status logs, or terminal text. `fm-task-activity.v1` carries the same metadata contract for its non-Web activity projection; `clerkmesh.execution-context.v1` is an internal machine-block contract, not a read projection. | `test:slice1-schema`, `test:slice2-clerk-repository`, `test:slice3-project-catalog`, and `test:slice5-learning`; `@clerkmesh/shared check:generated` inventories the versioned contracts |
| READ-005 | complete | `fm-task-graph.v1` projects a Clerk-agnostic Task boundary from Firstmate's authoritative fleet semantics. The guarded Web Task-detail endpoint queries that projection by Task ID and transiently enriches it only from the integrity-checked standard execution-context block in Firstmate's current brief, returning nullable immutable Clerk identity with explicit current-or-most-recent semantics and dual provenance; missing, malformed, mismatched, or symlinked briefs disclose no paths and never create ownership state | `test:slice2-clerk-repository` |
| READ-007 | complete | `test:slice3-project-catalog` proves production composition binds shared Task graph and Herdr Agent pollers to their public commands, starts the two-second schema/hash-gated queries only while the production Work client explicitly subscribes over the guarded WebSocket, publishes only schema-valid semantic fact changes (not per-query `observedAt` churn), and stops after the final subscriber disconnects; the built Work view composes Task and Agent runtime facts without private reads | A genuine Herdr/Treehouse E2E changed the live endpoint set from one to two; the production command, schema validator, semantic hash gate, two-second poller, and guarded WebSocket published the changed projection after 2050 ms without private endpoint or terminal fields; `evidence/slice-3/s3-004-real-web-polling.md` |
| READ-008 | complete | Polling query/schema failures publish a sanitized explicit error while retaining the last successful snapshot and its observation time; the production Work view keeps that snapshot visible and labels Task or Agent failures stale with the failed observation time rather than presenting current facts | A genuine isolated Herdr/Treehouse run produced one- then two-endpoint snapshots through the production command, poller, and guarded WebSocket; a controlled failure at that real query boundary then published only the sanitized error and later failure observation time while retaining the exact genuine two-endpoint snapshot, semantic hash, and success observation time. `corepack pnpm run cert:slice3-web-polling`; `evidence/slice-3/s3-004-real-web-polling.md` |
| READ-001 | complete | All four required v1 interfaces have strict shared schemas and generated TypeScript types; production Project catalog, Task graph, Herdr Agent, and bounded cursor-based Task activity projectors are covered for schema validity, current/stale/unknown freshness, omissions/errors, path-free output, and fail-closed malformed inputs | `corepack pnpm run cert:slice3-projections`; `evidence/slice-3/s3-003-firstmate-projections.md` |
| READ-006 | complete | `test:slice3-project-catalog` proves the Firstmate-owned append boundary emits only validated structured event types with contiguous cursors, Firstmate observation time, nullable genuine occurrence time, bounded summaries, durable fsync, and fail-closed task/history containment; malformed history remains unchanged. Ordinary ship and scout brief creation now appends `task-created` only after the brief exists, rolls the brief back if activity recording fails, and excludes non-Task secondmate charters. Successful ship/scout spawn now appends `dispatch-started` only after the launch is sent, fails visibly if durable recording fails, and excludes secondmate launches. Successful local-only fast-forward landing now durably appends `landed`, fails visibly if recording fails, and refused/diverged landing emits no false event. Successful ordinary Task teardown appends `teardown-recorded` before retiring volatile task metadata, preserves metadata for diagnosis if recording fails, excludes secondmate infrastructure, and refused teardown emits no false event. Authoritative Task diff review now durably appends `delivery-reviewed` after complete output and fails visibly if recording is unavailable. Creating or retrying a durable Captain hold appends exactly one `decision-requested` event per decision after the hold is active; failures remain visible and retries repair a missing append. Durable Captain decision resolution similarly appends exactly one idempotent `decision-recorded` event after routing and hold closure. Worker briefs now route status reports through a bounded Firstmate command that preserves the legacy status wake log and appends `worker-status-observed` with a Firstmate `observedAt` and no fabricated occurrence time; malformed summaries, unsafe storage, and symlinked logs fail closed. Accepted passed or failed Project validation results now use a bounded Firstmate command that appends `validation-recorded` with compact output, authoritative observation time, and fail-closed Task containment. Successful approved remote PR merge now appends `landed` only after `gh-axi` accepts the merge; forge refusal emits no false landing and post-merge activity failure remains visible | `corepack pnpm run cert:slice3-projections`; `evidence/slice-3/s3-003-firstmate-projections.md` |
| PROJ-001 | complete | `test:slice3-project-catalog` proves Firstmate projects and `data/projects.md` remain authoritative while its read-only projector preserves registered/discovered/missing semantics, validates managed directories and Git roots, and exposes no filesystem paths or second registry; the deterministic init command creates only beneath the managed Firstmate Project root and registers there last | The isolated genuine S3-001 lifecycle initialized and registered a disposable Project through the production boundary, then carried that Firstmate-owned managed repository through genuine Worker dispatch, validation, local landing, and teardown without creating a ClerkMesh registry or accepting an external path; `evidence/slice-3/s3-001-local-delivery.md` |
| PROJ-002 | complete | `test:slice3-project-catalog` proves an untagged registered Project defaults to `local-only off` in both dispatch preflight and the Project catalog, even when its Git repository has an origin; remote modes therefore require explicit registry authorization | Focused real-Git contract; ordinary local-only remains independent of forge readiness |
| PROJ-005 | complete | `test:slice3-project-catalog` proves deterministic local-only init creates `main`, a fixed README, a reproducible baseline commit, no remote, and publishes the Firstmate registry entry last; input/preflight refusals are zero-write and later failures retain a diagnosed managed path | The isolated genuine S3-001 lifecycle now starts through that production init boundary, verifies its exact branch/README/baseline/no-remote/registry results, and then carries the initialized Project through genuine Worker validation, review, approved landing, landed proof, and teardown; `evidence/slice-3/s3-001-local-delivery.md` |
| PROJ-006 | complete | `test:slice3-project-catalog` proves generated local-only Worker briefs exclude GitHub, PR tooling, `gh-axi`, and No Mistakes instructions while retaining Project validation and guarded fast-forward delivery requirements | The isolated genuine S3-001 lifecycle uses a production local-only spawn with no remote or forge dependency; its Pi Worker runs the Project-owned deterministic validation/delivery command, verifies the result and clean Git state, and produces the commit later reviewed and landed locally; `evidence/slice-3/s3-001-local-delivery.md` |
| PROJ-007 | complete | `test:slice3-project-catalog` drives real Git review, local landing, and teardown, proving the full diff is based on local `main`; `yolo=off` cannot land without the explicit `--captain-approved` gate action; `yolo=on` may auto-approve but cannot bypass clean fast-forward safety; unlanded work is preserved, and only safely landed task state is returned through the worktree-provider boundary | The isolated genuine S3-001 lifecycle preserves the Worker's Project-owned validation artifact and clean-Git result, discloses the authoritative full diff, refuses yolo-off landing without explicit approval, fast-forwards `main` to the exact reviewed tip, proves the output landed, and performs successful fail-closed Treehouse teardown; `evidence/slice-3/s3-001-local-delivery.md` |
| PROJ-008 | complete | `test:slice3-project-catalog` proves target-mode readiness is checked before registry mutation, failed switches preserve authoritative bytes, successful switches preserve the orthogonal yolo setting, and existing Task metadata remains fixed to its spawn-time mode while the registry changes for future dispatch. G0-004 separately proves new dispatch enforces the then-current mode before allocating worktree, endpoint, Worker, or metadata | The guarded S3-007 fixture exercised production mode publication and genuine `direct-PR` and `no-mistakes` readiness against only the Captain-authorized isolated remote, then completed real deliveries in both modes; the immutable existing-Task contract and future-dispatch switch semantics are covered without destructively rerunning the fixture; `evidence/slice-3/s3-007-remote-certification-blocker.md`, `evidence/slice-3/artifacts/s3-007-remote-foundation.txt`, `evidence/slice-3/artifacts/s3-007-real-direct-pr.txt`, `evidence/slice-3/artifacts/s3-007-real-no-mistakes.txt` |

| HUMAN-001 | complete | `test:slice4-human` freezes the injected Primary rule that a Human Clerk remains in the existing Firstmate Task/brief, must compile the approved Human commit with an explicitly empty material allowlist, and creates no Worker, wake, endpoint, capability, worktree, parallel human record, or synthetic Agent status; report publication requires a matching integrity-checked Human execution context; an isolated lifecycle integration proves accepted publication leaves only the authoritative brief/report and a fresh Task-detail composition recovers Human Clerk context without inventing Worker/Agent facts; Work explicitly presents Human execution as creating no Worker. | A genuine Pi Primary selected the ordinary Human Clerk, compiled its immutable empty-allowlist context, accepted Captain-relayed evidence, and created no Worker runtime artifact or queued wake; `evidence/slice-4/s4-001-human-execution.md` |
| HUMAN-002 | complete | `test:slice4-human` requires visible Captain-relayed start/progress/questions/evidence/result and requires Primary to invoke the bounded report command with exact relayed Markdown, explicitly evaluate every acceptance criterion, reserve `accepted` for satisfaction, and fail closed without a publication claim; the command atomically publishes non-empty Markdown with fixed local Captain actor provenance plus explicit `accepted|rejected|incomplete` evaluation; lifecycle integration preserves the accepted report across a refused empty replacement; invalid outcomes, empty results, unsafe Tasks, and symlinked reports fail without mutation | The genuine S4-001 run published the exact relayed Markdown with local Captain provenance only after evaluating both acceptance criteria; `evidence/slice-4/s4-001-human-execution.md` |
| HUMAN-003 | complete | `test:slice4-human` requires Project, Git, shell, or tool changes to preserve the accepted Human Task/report as dependency authority; actually create a separate Firstmate Agent Task through the pinned `tasks-axi` with `--blocked-by <human-task-id>`; verify the edge through Firstmate's Task projection; and restart candidates → shortlist → semantic Agent Clerk selection, Project preflight, and Agent context compilation before ordinary spawn. The isolated real-control-plane fixture proves a separate Agent Task is authoritatively blocked by the Human Task, the public graph exposes that exact edge, and a missing dependency is refused without backlog mutation. Any failed control-plane step stops before spawn, and prose or a parallel ClerkMesh dependency record is forbidden | `S4_003_LIVE=1 corepack pnpm run cert:slice4-human-agent-handoff`; tracked passing evidence: `evidence/slice-4/s4-003-human-agent-handoff.md` |
| HUMAN-004 | complete | The protocol explicitly distinguishes the Captain as the local relay rather than an account or Clerk; report publication records only fixed local decision provenance; Work describes only the current-or-most-recent execution Clerk and contract coverage rejects owner or assignment language | Genuine ordinary Human and explicit Escalation fixtures introduce no User, owner, assignment, or permission semantics; `evidence/slice-4/s4-002-human-escalation-selection.md` |

| LEARN-001 | complete | `test:slice5-learning` establishes the immutable content-addressed Source capture boundary: only explicit Captain import, accepted Human Task evidence, or explicit agent-generated re-import can persist; Agent completion/report/wake and extraction result origins fail before writes. Human provenance requires an accepted outcome and report hash, while explicit agent re-import is permanently warning-marked. Production CLI imports and accepted Human report integration are covered by `test:slice5-learning` and `test:slice4-human`. | A genuine Pi Human-to-Agent handoff fixture verifies exact accepted-report Source bytes, task/report hashes, and Captain-local provenance; `evidence/slice-5/s5-001-learning-source-capture.md` |
| LEARN-002 | complete | `test:slice5-learning` preserves `agentGenerated: true` plus a visible caution on an explicit Captain re-import and rejects treating ordinary Agent events as that explicit action; review materialization retains the provenance warning. | The tracked S5-001 evidence records warning-marked explicit re-import and automatic-learning refusals; `evidence/slice-5/s5-001-learning-source-capture.md` |
| LEARN-003 | complete | `test:slice5-learning` creates one authoritative Proposal over two distinct active Agent Clerks, fixes each target independently to its canonical Git HEAD, provisions isolated full candidate clones, and atomically advances all targets to extracting only after independent runtime endpoints are persisted. | Genuine Pi/Herdr extraction used two independently pinned targets; `evidence/slice-5/s5-002-real-learning-extraction.md` |
| LEARN-004 | complete | `test:slice5-learning` rejects non-Markdown, executable, binary, symlink, rename/copy, escaping, and wrong-base candidate output before review or approval. Candidate repositories are full no-hardlink clones isolated from Sources and canonical Clerk repositories. | Genuine two-target extraction produced independently validated Markdown-only candidate changes without canonical-repository mutation; `evidence/slice-5/s5-002-real-learning-extraction.md` |
| LEARN-005 | complete | `test:slice5-learning` atomically materializes per-target Source preview, changed paths, complete diff, validation, provenance warning, base commit, and candidate tree; edits invalidate review, canonical HEAD races mark only that target stale, and stale-target re-extraction starts from a fresh clone at current HEAD. | Reproducible review, stale-race, and re-extraction evidence is tracked in `evidence/slice-5/s5-003-learning-review-stale-reextract.md` |
| LEARN-006 | complete | `test:slice5-learning` proves independent exact-tree approval/rejection, post-review mutation and HEAD-race refusal, approved-target-only compare-and-swap commit advancement, rejected-target isolation, and terminal Proposal resolution. | An isolated test-Captain fixture captures both full diffs and independently approves one target and rejects the other; `evidence/slice-5/s5-004-independent-learning-decisions.md`, `evidence/slice-5/s5-006-test-captain-decisions.md` |
| LEARN-007 | complete | `test:slice5-learning` proves extraction is application-owned Proposal work rather than a Firstmate Task, uses a Proposal-dedicated Herdr workspace with independent target tabs/panes, and persists exact per-target endpoint authority before manifest transition. | Genuine Pi/Herdr extraction ran both targets independently and continued after the launch owner returned; `evidence/slice-5/s5-002-real-learning-extraction.md` |
| LEARN-008 | complete | `test:slice5-learning` reconciles persisted endpoint authority as live, complete, interrupted, or failed without relaunching Agents or inventing Captain decisions; stale targets can be independently re-extracted. | Genuine certification SIGKILLs the synchronized launch owner and uses a separate production reconcile invocation to recover both completed targets as review-ready; `evidence/slice-5/s5-005-restart-reconciliation.md`, `evidence/certifications/cert-005-learning-continuity.md` |

## Slice 2 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S2-001 | CLERK-001–005, LIFE-001–004 | isolated real-Git lifecycle integration, dirty-tree and recovery cases | Complete: valid independent repositories, dirty exclusion, lifecycle mutation, immutable commits, and real kill/restart recovery are covered |
| S2-002 | EXEC-001–003 | extension run/launch contracts plus real Pi certification | Complete: Slice 1 real Primary evidence plus concrete Firstmate Pi Worker exclusion contract |
| S2-003 | EXEC-004–008 | real Primary selection and canonical execution-context/brief verification | Complete: genuine Pi 0.82.0 selected among competing Clerks and produced an independently verified canonical context block fixed to the approved commit/blob |
| S2-004 | EXEC-010–012 | real Worker capability allowlist/refusal/audit certification | Complete: a genuine Herdr/Pi Worker used the immutable allowlist capability and produced content-free allowed/refused audit evidence |
| S2-005 | EXEC-009 | real follow-up and new-execution capability rotation | Complete: one genuine Pi session retained its immutable snapshot across follow-up and a distinct Herdr/Pi Worker rotated capability with cross-snapshot refusal |
| S2-006 | LIFE-005, READ-002–005 | runtime-validated catalog and Task-detail composition | Complete: focused certification proves the path-free catalog, Clerk-agnostic Task graph, guarded HTTP boundaries, and transient integrity-checked brief enrichment without Firstmate Clerk ownership fields |
| S2-007 | CLERK-004, EXEC-005–006 | real clarification and explicit Escalation selection transcript | Complete: genuine Pi Primary clarified unmatched work, waited after a requested preview, and selected Escalation only after explicit takeover without brief compilation |

## Slice 3 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S3-001 | PROJ-001, PROJ-002, PROJ-004–007, EXEC-012 | real local-only init → spawn → validate → review → approve → fast-forward → teardown | **Complete:** the isolated genuine Pi/Herdr/Treehouse runner produced a clean validated Worker commit, disclosed its authoritative diff, refused unapproved yolo-off landing, fast-forwarded the exact reviewed tip after explicit approval, proved landing, and safely tore down without remote, `gh`, or forge auth; `evidence/slice-3/s3-001-local-delivery.md` |
| S3-002 | PROJ-007, SEC-005 | isolated landing refusal/approval matrix for yolo off/on and unsafe changes | **Complete:** yolo-off refuses without explicit approval, approved landing succeeds, yolo-on still refuses divergent history, and a genuine Pi Primary awaited explicit Captain confirmation without reaching a destructive security-sensitive action; `evidence/slice-3/s3-002-yolo-escalation.md` |
| S3-003 | READ-001, READ-003–004, READ-006 | four schema-valid Firstmate projections with freshness/unknown/errors/cursors | **Complete:** `cert:slice3-projections` validates all four production projections against their v1 schemas, including current/stale/unknown freshness, omissions/errors, path-free output, activity continuation, malformed-history refusal, and the durable contiguous-cursor append authority; `evidence/slice-3/s3-003-firstmate-projections.md` |
| S3-004 | READ-007–008, CERT-003 | genuine Herdr + Treehouse projection and two-second hash-change polling | **Complete:** production Work subscriptions activate shared Task graph/Herdr polling, retain last successes, and visibly mark sanitized failures stale; a genuine isolated Herdr/Treehouse run changed endpoint facts from one to two and the production schema/hash-gated poller published the changed snapshot over the guarded WebSocket after 2050 ms; `evidence/slice-3/s3-004-real-web-polling.md` |
| S3-005 | PROC-002, OWN-001, READ-008 | separate Web/Primary/Worker exit and authority recovery scenarios | **Complete:** automated composition coverage proves Primary exit does not stop Worker projections, Web exit stops only process-local polling, and a fresh Web process recovers external Worker facts without inventing a Primary or completion. A genuine isolated Herdr/Treehouse run preserves the same two live endpoint facts and semantic hash across Web exit/restart without launching a Primary; genuine Pi S1-006 evidence separately proves Primary offline/no-restart and owned-child-only shutdown. `evidence/slice-3/s3-005-exit-recovery.md` |
| S3-006 | LIFE-003–004 | archive/restore lock, kill/restart journal, and CAS suite | **Complete:** the Slice 3 boundary certification repeats live-lock refusal, atomic archive/restore, reviewed-tree Git CAS race refusal, and a real post-journal SIGKILL followed by deterministic restart recovery; `evidence/slice-3/s3-006-clerk-lifecycle-recovery.md` |
| S3-007 | PROJ-003–004, PROJ-008, COMP-002/005, CERT-006 | isolated direct-PR/no-mistakes regression plus local-only independence | **Complete:** Project-add and mode-switch control planes enforce target preflight before atomic publication, refusal rollback, and existing-Task contract retention. Genuine runs baselined the private fixture, passed both production preflights, reviewed and squash-merged direct-PR PR #1, then drove the complete no-mistakes pipeline, verified certified-input ancestry and the full diff, recorded review, and squash-merged PR #2. See `evidence/slice-3/s3-007-remote-certification-blocker.md` and the three `s3-007-*` artifacts. |

Dependencies: Firstmate remains the sole Project/Task/delivery authority; the Herdr Agent projection reads Firstmate endpoint metadata and queries Herdr without exposing endpoint identities; pinned Treehouse v2.0.1 is available and the complete isolated genuine Pi/Herdr/Treehouse local delivery lifecycle passes (`evidence/slice-3/s3-001-local-delivery.md`); Web models must remain path-free and may not parse Firstmate private files.

Current bounded Project-add coverage: `test:slice3-project-catalog` proves an existing repository is cloned only into an unused managed path and its registry record is atomically published only after mode-specific readiness. Local-only add requires no forge tooling; direct-PR readiness is exercised against an isolated Git transport with `gh`/`gh-axi` tripwires; refusal preserves registry bytes and removes only the newly created clone. No-mistakes initialization is required before its shared readiness check.

Current bounded local delivery coverage: `PATH="$PWD/cache/bin:$PATH" S3_001_LIVE=1 corepack pnpm run cert:slice3-local-delivery` passed the tracked isolated genuine-runtime lifecycle for Pi/Herdr/Treehouse Worker validation, authoritative full-diff review, yolo-off refusal, explicit approval, exact-tip fast-forward landing, and landed teardown. The fixture exposes a Project-owned deterministic completion command so Worker execution and the Git delivery transition are one verifiable action rather than depending on probabilistic multi-step prompt compliance. The injected Primary protocol requires the same mode-specific `fm-project-preflight.sh` used by spawn before compile or spawn, fails before any execution-state mutation, and preserves local-only independence from forge probes. `test:slice3-project-catalog` proves local-only Worker briefs contain no GitHub, PR-tooling, `gh-axi`, or No Mistakes instructions while retaining validation and guarded fast-forward requirements. Valid Project inputs create a no-remote `main` repository with fixed README and deterministic baseline commit before atomically appending its `local-only` Firstmate registry entry. Invalid names, duplicate Projects, and malformed registry authority are refused before any Project write; post-creation failures retain and report the incomplete managed path. A real-Git review/landing contract additionally proves full diff disclosure, yolo-off explicit Captain approval, exact reviewed-tip landing by clean fast-forward, and yolo-on refusal after a divergent-main race. The teardown contract preserves unlanded work and task metadata without invoking Treehouse, then returns the worktree through the provider boundary and removes volatile task authority only after the exact tip is present on local `main`.

## Slice 4 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S4-001 | HUMAN-001–002, OWN-001–003 | isolated Human Clerk Task from visible start through accepted Markdown report, proving absence of Worker/endpoint/capability/worktree and synthetic Agent state | **Complete:** a genuine Pi Primary semantically selected the ordinary Human Clerk, compiled its approved commit with an empty allowlist, evaluated the relayed evidence, atomically published the exact accepted Markdown with Captain provenance, and created no Worker/runtime artifact or wake; `evidence/slice-4/s4-001-human-execution.md` |
| S4-002 | HUMAN-001, HUMAN-004, CLERK-004, EXEC-005–006 | ordinary human selection plus explicit Captain-takeover Escalation transcript | **Complete:** genuine Pi runs prove semantic ordinary Human selection and acceptance without runtime artifacts, while unmatched work remains in clarification and selects Escalation only after explicit Captain takeover; neither path introduces account, owner, assignment, or permission semantics; `evidence/slice-4/s4-002-human-escalation-selection.md` |
| S4-003 | HUMAN-003, EXEC-004, READ-005 | accepted human result followed by a dependent Agent Task and fresh Clerk selection | Passed: pinned real `tasks-axi` creation and the production Task projection prove the separate Agent Task's authoritative dependency edge and mutation-free invalid-dependency refusal; a genuine Pi Primary completed fresh Agent candidate selection, local-only preflight, and canonical context compilation. Evidence: `evidence/slice-4/s4-003-human-agent-handoff.md` |
| S4-004 | HUMAN-001–004, AUTO-005–006 | isolated implementation-Agent-as-test-Captain success, refusal, and restart fixtures retaining conversation, report, projections, and negative Worker evidence | **Complete:** genuine Pi success and refusal conversations plus focused report/recomposition recovery checks retain authoritative report and projection facts while proving no Worker runtime; `evidence/slice-4/s4-004-test-captain-paths.md` |
| S4-005 | PLAN-003, AUTO-005 | complete engineering evidence and tracked `awaiting-final-captain-uat` marker | **Complete:** all preceding Slice 4 exits have reproducible engineering evidence; Slice 4 is explicitly marked `awaiting-final-captain-uat` without gating Slice 5 or representing fixture decisions as Captain approval; `evidence/slice-4/s4-005-awaiting-final-captain-uat.md` |

Dependencies: Firstmate remains the only Task, brief, report, dependency, and delivery authority; Pi Session remains conversation authority; the Clerk approved commit and execution mode remain Clerk Git authority. Human execution must not invoke Herdr, Treehouse, Worker capabilities, or manufacture runtime facts. The bounded `clerk-human-report.sh` writes only the existing Task's `report.md` from Captain-relayed Markdown and records Primary's explicit acceptance evaluation; it does not infer acceptance or create runtime state. The existing S2-007 genuine Escalation evidence is reusable only for explicit-takeover selection, not as proof of the ordinary Human Clerk lifecycle.

## Slice 5 compact requirement-to-test matrix

| Exit | Requirements | Planned proof | Dependencies / status |
|---|---|---|---|
| S5-001 | LEARN-001–002 | explicit immutable Source capture plus automatic-learning refusal matrix | **Complete:** production explicit imports and accepted Human reports create immutable Sources; warning and automatic-origin refusal matrices pass; the genuine-Pi isolated fixture verifies exact report bytes and task/hash/Captain provenance; `test:slice5-learning`, `test:slice4-human`, `cert:slice4-human-agent-handoff`; `evidence/slice-5/s5-001-learning-source-capture.md` |
| S5-002 | LEARN-003–004, LEARN-007–008 | genuine two-target extraction with isolated Herdr workspaces and owner-exit continuity | **Complete:** authoritative immutable-base two-target Proposal creation, filesystem/Git-isolated candidate clones, fail-closed Markdown-only enforcement, and exact endpoint persistence pass. The production launcher binds Proposal/Source-derived non-Task Pi policy to one explicit session and one dedicated workspace with independent target tabs/panes. A reproducible genuine Herdr/Pi run proved both Agents continued after the launch owner returned and independently produced validated Markdown candidates without touching canonical Clerk repositories. Evidence: `evidence/slice-5/s5-002-real-learning-extraction.md`. |
| S5-003 | LEARN-005 | complete review, revision invalidation, HEAD race stale, and re-extraction | **Complete:** each target atomically persists complete review material and candidate edits create a new tree with review invalidation. Canonical HEAD races stale only the affected target and clear its review. Re-extraction creates a fresh isolated clone at current canonical HEAD, reuses the Proposal Herdr workspace, replaces endpoint authority, clears stale state, and returns only that target to extraction; `test:slice5-learning`; `evidence/slice-5/s5-003-learning-review-stale-reextract.md`. |
| S5-004 | LEARN-006 | independent approve/reject with approved-target-only CAS commit | **Complete:** reviewed targets independently persist Captain approval or rejection with exact base/tree decision evidence. Approval revalidates Markdown-only output, refuses post-review edits and HEAD races, creates the reviewed tree as a single-parent commit, and compare-and-swap advances only the approved Clerk branch; rejection leaves its Clerk unchanged. After all targets are decided, the authoritative Proposal becomes resolved and refuses further decisions. `test:slice5-learning`; `evidence/slice-5/s5-004-independent-learning-decisions.md`. |
| S5-005 | LEARN-008 | restart reconciliation for live, complete, and interrupted runs | **Complete:** authoritative endpoint reconciliation preserves live extraction, materializes completed output as review-ready, and records interrupted/failed targets without relaunching or inventing Captain decisions. Production Herdr inspection uses application-owned success markers, active runtime statuses, and absent endpoints fail-closed. Genuine Pi/Herdr certification SIGKILLs a synchronized launch owner, observes both extraction Agents complete independently, and recovers both targets from persisted authority; `cert:slice5-restart-reconciliation`; `test:slice5-learning`; `evidence/slice-5/s5-005-restart-reconciliation.md`. |
| S5-006 | AUTO-005–006 | isolated implementation-Agent-as-test-Captain evidence bundle | **Complete:** a reproducible temporary fixture reviews both complete diffs, approves one target, rejects the other, and emits Source hash, base/tree, diff, decision, and result-commit evidence while proving rejected-target and real-data isolation; `cert:slice5-test-captain-decisions`; `test:slice5-learning`; `evidence/slice-5/s5-006-test-captain-decisions.md`. |
| S5-007 | PLAN-003, AUTO-005 | engineering acceptance marker without fixture data leakage | **Complete:** all Slice 5 exits have reproducible engineering evidence, the focused suite passes, and Slice 5 is explicitly marked `awaiting-final-captain-uat`; isolated test-Captain decisions remain fixture-only facts and are not represented as Captain approval or business data; `evidence/slice-5/s5-007-awaiting-final-captain-uat.md`. |

## Stage status

- Gate 0: **complete**
- Slice 1: **complete** (S1-001 through S1-006)
- Slice 2: **complete** (S2-001 through S2-007)
- Slice 3: **complete** (S3-001 through S3-007)
- Slice 4: **awaiting-final-captain-uat** (S4-001 through S4-005 engineering exits complete; Slice 5 is not blocked)
- Slice 5: **awaiting-final-captain-uat** (S5-001 through S5-007 engineering exits complete)
- Certifications: **complete** (CERT-001 through CERT-006 have reproducible tracked genuine-runtime evidence)
- Release Gate: **in progress** (item 1 clean init, repeated init, and unknown-state refusal passes from a clean tracked root; `evidence/release-gate/rel-001-clean-init.md`; item 2 has exhaustive 119/119 requirement-to-test/evidence coverage with no unexplained skip; `evidence/release-gate/rel-001-requirement-coverage.md`; item 3 complete ClerkMesh automation and the 96-script Firstmate regression suite pass with zero failures; `evidence/release-gate/rel-001-aggregate-automated-tests.md`; item 4 genuine certification revalidation is in progress with fresh passing CERT-001 real Pi Web/TUI lock and CERT-002 Pi RPC/extension runs, with CERT-003 next; `evidence/release-gate/rel-001-certification-revalidation.md`)
